# Work Journal: 2026-06-02

## Session 00:00
### What was done
- Starting Task 2: SQLite Schema for English Learning Agent backend
- Following TDD approach: tests first, then implementation

### Current state
- About to create backend/db/__init__.py
- About to create failing tests in conftest.py and test_db_queries.py
- Then implement backend/db/schema.py

### Next steps
- Step 1: Create empty backend/db/__init__.py
- Step 2: Create conftest.py and test_db_queries.py
- Step 3: Run tests and verify they fail
- Step 4: Implement schema.py
- Step 5: Run tests and verify they pass
- Step 6: Commit changes

### Key decisions
- Used `Path.mkdir(exist_ok=True)` to safely create parent directories
- Enabled foreign key constraints with `PRAGMA foreign_keys = ON`
- Used `sqlite3.Row` factory for dict-like access in query results
- Stored JSON data (skills_progress, to_review, up_next, practiced, mistakes, mastered) as TEXT columns—will be parsed when needed
- Made id fields TEXT PRIMARY KEY for UUID compatibility

### Self-review findings
- All tests pass (2/2)
- Implementation matches spec exactly
- Foreign key constraints set up correctly for referential integrity
- Idempotent schema (uses CREATE TABLE IF NOT EXISTS)
- Proper context manager support (get_conn works with 'with' statement)
- Fixture correctly isolates tests with monkeypatch and tmp_path

### Tests passing
- test_init_db_creates_tables: PASSED (checks all 3 tables exist)
- test_init_db_is_idempotent: PASSED (checks schema can be initialized twice)

### Files created
- /backend/db/__init__.py (empty module file)
- /backend/db/schema.py (main schema module)
- /backend/tests/__init__.py (test package marker)
- /backend/tests/conftest.py (pytest fixture for db)
- /backend/tests/test_db_queries.py (2 initial tests)

### Commit
- Commit hash: de3e333
- Message: "feat(backend): SQLite schema and init_db"
