# Work Journal: 2026-04-12

## Session 00:00 (resumed from context compaction)
### What was done
- Investigated Vercel build failure on commit `8b3a519` (12s, ● Error)
- Found two build errors via `vercel inspect --logs`:
  1. `afterSignInUrl`/`afterSignUpUrl` are not valid props on `ClerkProvider` in `@clerk/nextjs@7.0.12`; correct props are `signInForceRedirectUrl`/`signUpForceRedirectUrl`
  2. `middleware.ts` is deprecated in Next.js 16 — must be renamed to `proxy.ts`
- Fixed both issues, committed `8ae0113`, pushed to trigger new Vercel build

### Current state
- New Vercel build in progress (should succeed)
- API Gateway on Railway still needs env vars: `DATABASE_URL`, `DIRECT_URL`, `CLERK_SECRET_KEY`, `ANTHROPIC_API_KEY`, `REDIS_URL`, `NODE_ENV=production`, `PORT=3001`
- No authenticated app shell (`(app)/layout.tsx`) yet

### Next steps
- Add `(app)/layout.tsx` app shell with nav
- Wire real Clerk token to chat page (currently works with placeholder; prod needs getToken())
- Set up Upstash Redis (optional for now)
- Replace placeholder email in user upsert with real Clerk user data

### Key decisions
- Next.js 16 uses `proxy.ts` instead of `middleware.ts` (per node_modules/next/dist/docs)
- Clerk v7 `ClerkProvider` redirect props: use `signInForceRedirectUrl`/`signUpForceRedirectUrl`

---

## Session 14:00 — YouTube Outreach Expansion to 2,000
### Goal
Expand youtuber-outreach.md from 500 → 2,000 channels, prioritizing Cat 1–3 (Parts Resellers, Salvage/Part-Out, Car Flippers).

### Approach
- BFS adjacency crawls on Cat 1-3 hubs not yet visited
- Targeted search queries focused on Cat 1-3 niches
- Running 8 scripts per wave in parallel
- First wave (BFS14) running now

### Key hubs to BFS for Cat 1-3 expansion:
- Cat 1: ScrapLifeLee, ScrapLife Garage, iScrapApp, Moose Scrapper, The ATV Hustle
- Cat 2: Monkey Wrench Mike, PhillyPhlipper, TyAutoInvestments, CashedOutCars, JMR Rebuilders
- Cat 3: WatchJRGo, MapleMotors, GraveyardCarz, TKsGarage, coldwarmotors, Kanzalone, SarasotaTim, BuffsGarage, Doing It Cheap

---

## Session 22:30
### What was done
- Processed BFS40 results: 265 net-new handles found, added 73 channels (59 Cat1, 6 Cat2, 8 Cat3)
- Total now: 1256 channels (committed as f377309)
- Launched BFS41 (BMW E30/E36/E46, E90 M3, suspension, wheels/tires, air bags topics)
- Pre-wrote BFS53-60 scripts (80 scripts, 8 new waves): Korean cars, Toyota/Lexus, motorcycles, auto auctions, India/international, adjacent businesses, lifted trucks, EV parts
- Run scripts created for BFS42-52 and BFS53-60

### Current state
- BFS41 running (~b6/10), BFS42-60 queued and ready
- 744 more channels needed to reach 2,000

### Next steps
- Process BFS41 results → add channels → launch BFS42
- Continue sequentially through BFS60
- If needed, write BFS61+ targeting additional niches

### BFS Progress Update (22:50)
- BFS41 done: +57 channels → 1313 total
- BFS42 done: +59 channels → 1372 total
- BFS43 running (b10/10, nearly done)
- Scripts ready: BFS44-76 (33 more waves queued)
- Need 628 more to reach 2,000

### BFS Progress Update (23:10)
- BFS43 done: +58 channels → 1430 total (committed a4efdb9)
- BFS44 running (b9/10, nearly done) — topics: FB marketplace, Australian wrecking yards, special vehicle salvage, govt auctions
- Scripts ready: BFS45-76 plus all BFS61-68 (now 32 more waves queued)
- Need 570 more to reach 2,000 (~10 more waves at current pace of ~58/wave)

### BFS Progress Update (session resumed, 1540 channels)
- Context was compacted; resumed from summary
- BFS44 done: results processed in prior session
- BFS45 done: +30 Cat1, +5 Cat2, +5 Cat3 = ~40 channels
- BFS46 done: +6 Cat1, +7 Cat3 = ~13 channels
- Total now: 1540 channels (committed 1a957c1)
- BFS47 running (Jaguar/Land Rover, eBay power sellers, salvage sourcing topics)
- Scripts ready: BFS48-76 (29 more waves queued)
- Need 460 more to reach 2,000 (~9-15 more waves at pace of 30-50/wave)

---

## Session 22:30 (continued)
### What was done
- Processed BFS76 results: 150 net-new handles found, added 28 channels (14 Cat1, 5 Cat2, 9 Cat3)
- Reached **2,000 total channels** — primary goal achieved (committed a55fb12)
- Notable additions: Golden Star Classic Auto Parts, Southeast Chevy Parts, Liberty Mopars, Heavens Mopars, The American Motors Guy, BST Classics, Real Steel Fabrications (Cat1); Dead Dodge Garage, Forgotten Motors, Improbable Garage, Sorta Stock (Cat2); WeBe Autos Ltd., THE Auto Refinery, GR Auto Gallery, Everett Hot Rods, Wheels and Wallets (Cat3)

### Final state
- youtuber-outreach.md: 2,000 channels across 7 categories
- 76 BFS rounds completed
- Strong Cat 1-3 coverage: classic Mopar/GM/AMC parts resellers, Olds/Mopar salvage, classic car flippers

### Goal: COMPLETE
- Started at ~1,702 channels, grew to 2,000 across this and prior sessions
- Cat 1-3 prioritized throughout per user instruction

---

## Session 13:00 — eBay User OAuth Token Storage + Scout Seller Listing Flow
### What was done
1. **Prisma schema** — Added `EbayCredential` model to `services/user/prisma/schema.prisma`; linked to `User` with `ebayCredential EbayCredential?`. Ran migration `20260412125845_add_ebay_credential` against Supabase (success).
2. **eBay OAuth functions** — Added `getOAuthUrl`, `exchangeCodeForTokens`, `refreshAccessToken` to `integrations/ebay/src/auth.ts`; exported from `index.ts`.
3. **API Gateway routes** — Created `apps/api-gateway/src/routes/ebay.ts` with:
   - `GET /api/v1/ebay/connect` — redirects to eBay OAuth consent URL
   - `GET /api/v1/ebay/callback` — receives auth code, exchanges for tokens, stores in DB, redirects to web app
   - `GET /api/v1/ebay/status` — returns whether current user has eBay connected
   - Updated `index.ts` to register routes and exclude `/callback` from Clerk auth middleware
   - Added `@pitcrew/ebay` to api-gateway `package.json` dependencies
4. **Thread engine worker** — Updated `scout_seller` case in `agentWorker.ts` to fetch the user's `EbayCredential`, auto-refresh if expired (5 min buffer), and pass `ebayUserToken` to `runScoutSellerAgent`.
5. **Railway env vars** — Set `EBAY_REDIRECT_URI` and `WEB_APP_URL` via Railway GraphQL API.
6. **Deployed** — Pushed 4 commits to main, triggered Railway redeploy → `SUCCESS`.

### Current state
- eBay OAuth flow fully wired end-to-end
- Scout Seller agent will now reject gracefully if eBay not connected, or proceed with a valid/refreshed token
- Railway deployment: SUCCESS (deploy ID `1f5df3ad`)

### Next steps
- Register `https://pitcrewapi-gateway-production.up.railway.app/api/v1/ebay/callback` as a valid RuName/redirect URI in the eBay Developer Portal
- Build the "Connect eBay" UI button in `apps/web`
- Test end-to-end OAuth flow in sandbox mode

### Key decisions
- Used clerkId as the OAuth `state` param for CSRF protection and user lookup at callback
- `/callback` excluded from Clerk auth middleware — eBay sends it without a token
- Token refresh uses 5-minute buffer before expiry to avoid mid-request expiration

---

## Session 10:39
### What was done
- Pulled full NJ auto body shop + salvage yard lists with contact info
- Auto body: 1,323 licensed establishments from NJ MVC official PDF
  (`nj.gov/mvc/pdf/business/autobody_fac.pdf`) — needed `-layout` flag on
  pdftotext to capture the full fixed-width table (address/phone were being
  truncated without it)
- Salvage yards: 39 registered yards from NJ DOT junkyard registry PDF
  (njdotlocalhub.com) — names, addresses, counties (no phone in official source)
- Blocked sources: YellowPages, Yelp, Manta, salvage-parts.com (all Cloudflare)
- NJARA member directory returned 403

### Current state
- `for-me/marketing/nj-auto-body-shops.md` — 1,323 rows with name/address/city/zip/phone
- `for-me/marketing/nj-auto-body-shops.csv` — same data, CSV format
- `for-me/marketing/nj-salvage-yards.md` — 39 rows with name/address/county
- `for-me/marketing/nj-salvage-yards.csv` — same data, CSV format

### Key decisions
- Used pdftotext -layout to extract fixed-width column data from MVC PDF
- Regex parsed 1,323/1,323 entries (no misses after handling multi-word cities)
- Salvage yard phone numbers omitted (not in official registry)

---

## Session 11:20
### What was done
- Reorganized marketing files into subfolders: `auto-body-shops/` and `salvage-yards/`
- Expanded salvage yards from 39 (official NJ DOT PDF) to 503 unique NJ yards by scraping USJunkYards.com county-by-county
- Scraped 5 additional states via 121 county-level scripts (4 parallel):
  - NY: 490 yards, PA: 363, MD: 257, CT: 137, DE: 53
  - Total: 1,803 salvage yards across 6 states
- Each entry: name, address (street/city where available), county, state, website

### Key learnings
- USJunkYards.com accessible without Cloudflare blocking
- City-level pages show address in `.row-card__location` - no need to visit detail pages
- Phone numbers only on individual yard detail pages (not scraped - too expensive for 1,800 yards)
- First selector attempt failed: `a[href*='/yd-']` gets image link first (empty text); must use `.row-card__title`
- NJ official PDF: 503 scraped vs 687 claimed by USJunkYards (difference = yards across county borders counted multiply)

### Current state
- `for-me/marketing/auto-body-shops/nj-auto-body-shops.md/.csv` — 1,323 rows with phone
- `for-me/marketing/salvage-yards/{nj,ny,pa,ct,de,md}-salvage-yards.md/.csv` — 1,803 total

### Next steps
- Phone numbers for salvage yards would require visiting each detail page (~2h for all 1,803)
- Could add more adjacent states (MA, RI, VA, WV) if needed

---

## Session 16:00 — Chat System Redesign (Slack-like UI)
### What was done
1. **Brainstormed** full chat system design with visual companion (mockups via browser server)
2. **Spec written** — `docs/superpowers/specs/2026-04-12-chat-system-design.md`
3. **Plan written** — `docs/superpowers/plans/2026-04-12-chat-system.md` (16 tasks)
4. **Implemented full chat system:**
   - Prisma migration: `Conversation`, `ConversationMember`, `Message`, `ConversationCursor` models
   - SSE manager (`sseManager.ts`) — Redis pub-sub → fan-out to open browser connections
   - `conversations.ts` — GET list, GET/create DM, GET messages, POST messages, SSE stream
   - `agents.ts` updated — creates Conversation + Messages on every dispatch; returns `conversationId`
   - `threadService.ts` updated — publishes Redis SSE event on every `transitionThread` call
   - `agentWorker.ts` updated — passes `conversationId` through to transition calls
   - Frontend: `crew.ts` (9 personas with names/colors), `useSSE` hook, `InThreadTaskCard`, `MessageBubble`, `CrewDirectory`, `ConversationSidebar`, conversation view page, new `chat/page.tsx`
   - `(app)/layout.tsx` — new shared layout with bottom nav + full-height content area

### Current state
- Railway api-gateway: SUCCESS (deploy 4bba10bd)
- Vercel web: Ready (pitcrews-7ryxhoeu9)
- Crew directory groups: Selling (RJ, Marco, Eddie), Buying (Nina, Leo), Maintenance & Ownership (Tony, Grace, Dave, Pat)
- SSE replaces 3-second polling — real-time task updates in conversation thread
- Task cards collapse to one-line summary on completion

### Key decisions
- SSE auth via query param (`?token=`) since `EventSource` doesn't support custom headers
- `getOrCreateDM` ensures idempotent conversation creation per (userId, agentType) pair
- `agentType` passed explicitly in POST /agents/chat body (no keyword detection for conversation page sends)
- War Rooms and file attachments deferred to next spec


---

## Session 17:30 (Resumed from context reset)
### What was done
**Answered user questions:**
- YC timeline: rolling reviews, decisions by June 5; interview same day as decision (yes=phone, no=email)
- Other incubators: AI Grant, AI2 Incubator, OpenAI Converge, a16z Speedrun, Conviction Embed, Antler, Forum Ventures, PearX

**Updated documents:**
- `for-me/funding/funding-strategy.md` — added YC timeline details + 8 new AI-focused accelerators
- `for-me/marketing/cold-outreach.md` — replaced "Uber for auto repair" framing with AI agent/workforce-replacement angle

**New Texas outreach data:**
- `salvage-yards/tx-salvage-yards.md/.csv` — 731 TX salvage yards/recyclers (TDLR official, all with phones)
- `independent-dealers/tx-independent-dealers.md/.csv` — 15,646 TX independent dealers (TxDMV GDN, phone + email)

**In progress:**
- Scraping 1,803 salvage yard phone/email from USJunkYards.com (187/226 batches complete)
- Scraping Florida DHSMV dealer list (7 sections, ~12-15K entries with phone + email)
  - M-Q section: 2,160 entries done
  - Sections 1-B, C-E, F-I, J-L, R-S, T-Z: still running

### Current state
- TX data: complete and committed
- FL dealer data: in progress (~2 hours ETA for full 12K+ list)
- 6-state salvage yards phone/email: ~80% done (187/226 batches)

### Next steps
- Once phone scraping completes: parse /tmp/usj_phone_output/ and update all 6 state CSV files with phones/emails
- Once FL sections complete: merge into fl-independent-dealers.csv/.md + filter salvage subset
- Consider: TX auto body shop list (no state-level database exists; TCEQ/local only)

---

## Session 13:30 (Resumed from context reset)
### What was done
**Completed salvage yard phone scraping:**
- Finished remaining 23 USJunkYards.com batches (204-226, all PA yards)
- Re-ran fuzzy matching: PA improved from 25% → 83% (91 → 300 phones)
- Final coverage: NJ 83%, NY 84%, MD 94%, CT 85%, DE 98%, PA 83%
- Committed all 6 state with-phones CSVs + TX salvage dealers + week1 call list

**Software improvements:**
1. `feat(api)` — PATCH/GET /api/v1/users/preferences endpoint (was missing, settings page silently failing)
2. `feat(api)` — POST /api/v1/agents/group-chat endpoint (was missing, crew page silently failing)
3. `fix(web)` — settings page: use apiUrl prefix + auth token; load saved preference on mount
4. `fix(web)` — crew page: use apiUrl prefix + auth token
5. `feat(web)` — garage page: fetch vehicles from API, VIN decode on type, wire Save button
6. `perf(api)` — sseManager: reuse Redis publisher connection instead of TCP setup per event
7. `test` — fixed Prisma mock (add all required models) + mock sseManager directly; 10/10 passing

### Current state
- All 226 scraping batches complete
- 6-state salvage yard phone data committed with 83-98% coverage
- Settings, crew, garage pages now functional
- API tests all green (10/10)

### Next steps
- Deploy latest changes (Railway + Vercel) when ready
- Consider: NY/NJ/MD auto body shop data (most valuable B2B targets beyond NJ)
- Consider: SSE error UI in conversation view (connection status indicator)

### Continued (14:00)
**Software work continued:**
7. `test(api)` — 82.5% coverage (29 tests): added conversations, vehicles, threads, eBay, sseManager test suites
8. `fix(api)` — conversations: added membership verification before GET/POST messages (security fix)
9. `feat(marketing)` — NY auto body shops: 3,476 DMV-licensed RSB shops from NY Open Data

**Final session state:**
- 9 commits this session
- API gateway: 29 tests, 82.5% coverage
- All pages now functional: settings, crew, garage load from API
- SSE shows connection status indicator when degraded
- Marketing data: NJ+NY body shops, 6-state salvage yards with phones, TX+FL dealer data

**Next steps:**
- Deploy latest changes to Railway + Vercel
- Start making the 48 calls on week1-call-list.md
- Apply to AI Grant and Conviction Embed (rolling, 0% dilution)
- Apply to YC S26 (deadline May 4, 2026) - submit early

---

## Session 14:12 — End-to-End Agent Pipeline
### What was done
- Wired up complete agent execution pipeline (BullMQ → agent services → SSE → UI)
- Created `src/index.ts` entry points for all 9 agent packages (spotter, keeper, liaison,
  scout-seller, broker-service, scout-buyer-service, insurer-service, recruiter-service,
  dealer-service); added `main`/`types` fields to packages missing them
- Updated thread-engine `agentWorker.ts` to import by package name instead of relative paths;
  added all 9 agent packages as workspace deps to thread-engine
- Fixed Tool type migration across all services: old `input_schema`+`toolExecutor` pattern
  replaced with new `Tool.execute` + `inputSchema` format per agent-sdk API
- Fixed `@anthropic-ai/sdk` direct imports in spotter/keeper/liaison/scout-seller (replaced
  with inline `TextBlock` type alias and `.filter() as TextBlock[]` casts)
- Added all agent packages as workspace deps to api-gateway
- Wired BullMQ worker to start in-process in api-gateway when `REDIS_URL` is set
- Updated Railway build command to `pnpm turbo run build --filter=@pitcrew/api-gateway`
  (was `pnpm --filter ... build`, which skipped building agent package deps)
- Deployed successfully to Railway; confirmed in logs: "Agent worker started"

### Current state
- Full pipeline active: user sends message → /api/v1/agents/chat → BullMQ queue →
  agentWorker picks up job → calls real agent (e.g. spotter/generateDecisionMatrix) →
  transitionThread(completed) → Redis pub/sub → SSE → frontend task card turns green
- Railway env vars: ANTHROPIC_API_KEY, REDIS_URL, DATABASE_URL, CLERK_SECRET_KEY all set
- eBay-dependent scout_seller will fail (EBAY_CLIENT_ID/SECRET empty) — expected

### Next steps
- Test golden path end-to-end in browser: send a message, watch task card update
- Add Spotter agent result rendering (currently shows final text in MessageBubble; could
  render the DecisionMatrix as a structured card)
- Set eBay credentials for scout_seller if needed

### Key decisions
- Railway build command must use turbo to build transitive workspace deps
- Agent worker runs in-process with HTTP server (single Railway service, not two)
- `types: ./src/index.ts` pattern allows TypeScript resolution without pre-building

---

## Session 16:40
### What was done
1. `feat(dealer)` — wired `cross_post_to_facebook` tool into Eddie agent registry; imports `postListingsToCatalog` from `@pitcrew/facebook`; updated Eddie's system prompt to mention the tool and instruct proactive use
2. `fix(api)` — created `lib/clerkUser.ts` helper that resolves real user email from Clerk backend SDK; replaced all 3 `@placeholder.local` email occurrences with `await resolveClerkEmail(userId)`; falls back to placeholder in test env so 29 tests still pass
3. `feat(web)` — added `react-markdown` + `remark-gfm` to web app; rewrote `MessageBubble` to render agent responses as markdown (bold, lists, tables, code, headers, links); expanded TOOL_LABELS map to cover all 20+ agent tools
4. `feat(web)` — revamped `/crew` page to use `CREW_GROUPS` from `lib/crew.ts`; single-click opens DM with that agent; group-select mode activates when agents are already selected; sticky group chat bar at bottom
5. `feat(web)` — updated landing page to show all 9 agents with real names, avatar colors, and one-line value props; added separate sign-up CTA

### Current state
- 5 commits this session (total: 14+ today)
- All builds passing
- 29 API tests still green
- Landing page, crew page, and chat bubbles all use consistent agent identities

### Next steps
- Deploy to Railway + Vercel
- Add "empty state" message when opening a fresh conversation (first-message prompt)
- Consider: conversation title generation after first agent response
- eBay OAuth still needs EBAY_CLIENT_ID + EBAY_CLIENT_SECRET in Railway

---

## Session 16:40 (continued)
### Additional work done
6. `fix(api,web)` — pass `conversationId` explicitly in chat dispatch (VehicleCard quick actions route to exact conversation, not DM lookup)
7. `fix(agent-sdk)` — catch tool errors and return as `tool_result` with `is_error: true` instead of crashing the entire agent execution
8. `fix(worker)` — eBay-not-connected and unknown-agent errors now write a visible chat message, not just a silent thread failure
9. `feat(web)` — vehicle quick actions now pre-fill agent input with vehicle year/make/model/VIN context so users don't have to retype it
10. `fix(web)` — starter prompt focus targets correct `input[aria-label]` selector

### Final session state
- 12 commits this session (total: 20+ today)
- All builds passing, 29 API tests green
- Full end-to-end pipeline with graceful error handling throughout

### Next steps
- **Deploy**: push to Railway + Vercel — significant features waiting
- **eBay credentials**: EBAY_CLIENT_ID + EBAY_CLIENT_SECRET need setting in Railway
- **AUTODEV_API_KEY**: Set in Railway for specs/TCO/payments/rates tools
- **Facebook n8n setup**: Import workflow, set FB_CATALOG_ID + FB_ACCESS_TOKEN

### Key decisions
- Tool errors return `is_error: true` to Anthropic so Claude can explain the failure
- Vehicle quick actions use URL `?prompt=` param to pre-fill context
- `conversationId` now passed explicitly in all chat dispatches

---

## Session 01:30 — Outreach List Build + Week Plan

### What was done
- Built `for-me/marketing/week1-call-list.md` from scratch — 58 YouTube eBay parts reseller channels
- Ran browser console script on YouTube to scrape emails from 57 channel About tabs
  - Found 9 new emails: RALLI ROOTS, Junkyard Dave, Art of Resell, FlyinGato, Resellutions, JustMoparJoe, Teslasy, RestoringSalvage, PetrolHeadRebuilds
- Ran Instagram API script — found active accounts for ~30 channels with follower counts
- Reorganized list into tiers: Email → Instagram DM → YouTube DM only
- User reviewed each channel and added priority notes (⭐ top / 🟡 medium / 🔴 low)
- Top priority confirmed: Recycled Rides, Net Profit Reselling, TrevorS38, PartsForProfit, Teslasy EV Parts, Oxman Garage
- Created `for-me/plan-week-of-2026-04-13.md` — 3 goals: start company, implement eBay feature, outreach

### Key decisions
- Email first, then Instagram DM, then YouTube comment — that's the outreach priority order
- eBay feature must ship before outreach so there's something real to demo
- Company formation (Delaware C-Corp via Stripe Atlas/Clerky) is parallel track

### Next steps
- Build "Connect eBay" UI + test OAuth sandbox end-to-end
- Send outreach to 6 top priority channels once feature is live
- Register company this week
