# Work Journal: 2026-04-06

## Session 09:00

### What was done
- Completed full brainstorming session → design spec approved
- Spec at: docs/superpowers/specs/2026-04-05-ai-business-platform-design.md
- Starting relentless implementation

### Current state
- Empty project directory — spec only
- Phase: Implementation start

### Key decisions
- Next.js 14 (App Router) + TypeScript + Tailwind + shadcn/ui
- Claude API (claude-sonnet-4-6) per employee role
- Prisma + PostgreSQL for persistence
- BullMQ for background job queue
- Anthropic MCP SDK for per-role tool sets

### Cycle 1 (starting ~09:00)
Target: Project scaffold + Prisma schema
Done criteria: App boots, all DB models defined

---

## Session wrap-up (~12:00)

### What was done
- **Cycle 1**: Full project scaffold — Next.js 16 + Prisma 7 (pg adapter) + 35 employee role configs, Claude streaming chat service, all API routes, landing page, onboarding chat, dashboard
- **Cycle 2**: Daily Briefing service + API + DailyBriefing popover component in dashboard header
- **Cycle 3**: Legal intake 3-step form, legal workflow API, LegalModal in dashboard, POST /api/employees/hire, sidebar hire dropdown
- **Cycle 4**: README setup guide, dev server verified (starts in 222ms, serves landing page)

### Current state
- **Build**: passes clean (11 API routes, 0 TypeScript errors)
- **Dev server**: boots in <1s
- **DB**: schema defined, Prisma client generated — needs `prisma migrate dev --name init` against a live Postgres to run
- **Working**: all UI components, all API routes, CEO Agent onboarding conversation
- **Not yet wired**: actual Anthropic API calls need `ANTHROPIC_API_KEY` in `.env`; Prisma DB calls need live Postgres

### Key decisions
- Prisma 7 breaking change: removed `url` from schema, moved to `prisma.config.ts`. Used `@prisma/adapter-pg` for client constructor
- Zod v4 breaking change: `.errors` → `.issues`
- CEO Agent system prompt is enriched with Company Memory on every call (injected as context, not persisted)

### Next steps
1. Set up Postgres and run `prisma migrate dev --name init`
2. Add `ANTHROPIC_API_KEY` to `.env`
3. Test the full onboarding → dashboard flow end-to-end
4. Phase 2: add Task management API + task assignment from chat
5. Phase 2: add real MCP tool execution per employee role
6. Phase 2: brand kit generation (image generation API → logo)

### Next action
Run `prisma migrate dev --name init` once Postgres is available, then test full user flow at http://localhost:3000

---

## Session 01:10 (context recovery — continued from previous session)

### What was done

**Cycle 1 — Auth system**
- Added `User` model to Prisma schema, linked `Business → User` (userId nullable for backwards compat)
- Ran migration: `20260406050032_add_user_model`
- JWT session management via `jose` HS256 + httpOnly cookie (7-day expiry)
- Server Actions: `signup`, `login`, `logout` in `src/app/actions/auth.ts`
- Sign-up page `/signup` and login page `/login` with Zod validation + bcrypt password hashing
- Route protection via `src/proxy.ts` (Next.js 16 renamed `middleware` → `proxy` with `export function proxy`)
- Admin routes gated by `isAdmin` flag in JWT payload
- Logout button in dashboard header
- Landing page updated with Sign in / Start Building CTAs
- Fixed `src/generated/prisma/index.ts` barrel export (was missing, blocking build)

**Cycle 2 — Admin dashboard**
- Admin page at `/admin`: platform-wide stats cards (users, businesses, employees, messages)
- Expandable user rows showing each user's businesses, employee counts, role badges
- `GET /api/admin/stats` — full usage data, admin-gated
- `POST /api/admin/promote` — promote user to admin (bootstrap-safe: first call allowed if no admins exist)
- Proxy guards `/admin` routes to `isAdmin` sessions

**Cycle 3 — Expo mobile app** (at `/workspace/buildco-mobile`)
- Full React Native/Expo app mirroring web
- Screens: Landing, Signup, Login, Onboard (CEO Agent chat), Dashboard (employee roster), Chat (SSE streaming)
- Auth: JWT via `expo-secure-store`, Bearer token on all API calls
- Navigation: React Navigation native stack, auth-gated routing
- Added REST auth endpoints to web backend: `POST /api/auth/signup`, `POST /api/auth/login`
- `getSession()` updated to accept optional `bearerToken` param (accepts both cookie and Bearer)

### Current state
- **Web build**: clean, 17 API routes, no errors
- **Mobile**: TypeScript clean, ready to run with `npm start` in `/workspace/buildco-mobile`
- **DB**: live Postgres running, migrations applied, real Claude API calls verified working
- **Auth**: full sign-up/login/logout flow for web; REST token auth for mobile
- **Admin**: accessible at `/admin` (must be promoted via `POST /api/admin/promote`)

### Key decisions
- Next.js 16 renamed `middleware.ts` → `proxy.ts` and `export function middleware` → `export function proxy`
- Mobile auth uses Bearer JWT (not cookies); backend `getSession()` accepts both
- Admin bootstrap: first promote call is allowed when `adminCount === 0` (no chicken-and-egg)

### Next steps
1. Test full web flow: sign up → onboard → dashboard → employee chat
2. Test admin: create account, `POST /api/admin/promote`, visit `/admin`
3. Run mobile: `cd buildco-mobile && npm start`, open in iOS Simulator
4. Phase 2: Task management (create/assign tasks from CEO Agent chat)
5. Phase 2: Daily briefing refresh + push notifications (mobile)
6. Phase 2: Brand kit generation (AI-generated logo via image API)

### Next action
Test the full flow end-to-end at http://localhost:3001
