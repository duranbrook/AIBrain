# Work Journal: 2026-04-22

## Session 09:00
### What was done
- Explored full repository structure (backend, web, android, ios, docs)
- Created root-level README.md with a comprehensive quick start guide

### Current state
- Project is a multi-platform AI-powered auto shop inspection platform (PitAgents)
- Backend: FastAPI + LangGraph agent, partially using in-memory stores (not yet full DB integration)
- Web: Next.js 16 dashboard built
- Mobile: iOS (Swift/AVFoundation) and Android (Kotlin/CameraX) apps scaffolded
- Docker Compose setup exists for local development

### Next steps
- No further work requested this session

### Key decisions
- README targets new developers onboarding to the project

---

## Session 23:09
### What was done
- Brought up full Docker Compose stack (db, redis, migrate, backend, web)
- Fixed 5 startup bugs blocking the app from running:
  1. `pydantic_settings` rejecting unknown env vars (Twilio keys in `.env`) → added `extra="ignore"` to `Settings`
  2. `passlib 1.7.x` incompatible with `bcrypt 4.x` → pinned `bcrypt<4.0` in pyproject.toml
  3. Missing `pydantic[email]` dependency → added to pyproject.toml
  4. Root `/` page was default Next.js scaffold → now redirects to `/dashboard`
  5. No CORS middleware → browser blocked all API calls; added `CORSMiddleware` for `localhost:3000`
- Added `/login` page with JWT flow (stores token in localStorage, redirects to dashboard)
- Added auth guard on dashboard (redirects to `/login` if no token)

### Current state
- All containers healthy: db, redis, backend (port 8000), web (port 3000)
- Login works with test credentials: `owner@shop.com` / `testpass`
- Dashboard loads and displays reports from the DB

### Next steps
- Reports show "—" for Vehicle, Total, Date — test data is missing these fields
- No logout button yet
- No way to create new inspection sessions from the web UI

### Key decisions
- Kept `extra="ignore"` rather than removing Twilio keys — they may be used by backend tools not yet wired to Settings
