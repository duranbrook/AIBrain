# Work Journal: 2026-04-23

## Session 00:00
### What was done
- Resumed from previous session (context compacted)
- Previous session completed: design spec + 15-task implementation plan for Web Chat UI (Sub-project 1)
- Starting Subagent-Driven Development execution of the plan

### Current state
- Plan: `docs/superpowers/plans/2026-04-23-web-chat-ui.md` (15 tasks)
- Branch: `main` (worktree to be created before implementation starts)
- Docker stack healthy: db, redis, backend (8000), web (3000)
- Login works: `owner@shop.com` / `testpass`

### Next steps
- Set up git worktree (feat/web-chat-ui branch)
- Execute all 15 tasks via subagent-driven development

### Key decisions
- Using `chat_messages` SQLAlchemy table instead of LangGraph `AsyncPostgresSaver` (simpler, avoids psycopg v3 dependency)
- Two agents: Assistant (VIN tools) and Tom (shop DB tools)
- SSE streaming from backend, MediaRecorder for voice in browser

---

## Session 12:00 (resumed after context compaction)
### What was done
- Resumed execution of 15-task Web Chat UI implementation plan (feat/web-chat-ui branch)
- Previously completed: Tasks 1-8, 10, 11, 12 (DB model, transcribe, upload, agent tools, base, assistant/tom, chat API, frontend API client, MessageBubble, VoiceButton, ImageAttach)
- Continuing with Task 13: ChatPanel component

### Current state
- Branch: feat/web-chat-ui in .worktrees/feat-web-chat-ui
- Remaining tasks: 13 (ChatPanel), 9 (AppShell+AgentList), 14 (/chat page), 15 (smoke test)

### Next steps
- Task 13: ChatPanel.tsx
- Task 9: AppShell + AgentList
- Task 14: /chat page + redirect
- Task 15: E2E smoke test + docker rebuild

### Milestones completed (session resumption)
- Task 13: ChatPanel component (SSE streaming, voice/image input, accumulator fix for stale closure)
- Task 9: AppShell + AgentList components (WhatsApp/Slack-style layout, accessibility fixes)
- Task 14: /chat route + root redirect
- Task 15: End-to-end smoke test — 76/76 backend tests passing, all containers healthy

### Current state
- SUB-PROJECT 1 COMPLETE: All 15 tasks implemented and reviewed
- Stack running on ports 3000 (web), 8000 (backend), 5432 (db), 6379 (redis)
- Branch: feat/web-chat-ui — 27 commits ahead of main
- Frontend: http://localhost:3000 → redirects to /chat

### Key fixes during Task 15
- `.env` had `SENDGRID_FROM_EMAIL` not in Settings model — removed from worktree .env
- `bcrypt` 5.0.0 incompatible with `passlib` 1.7.4 — pinned `bcrypt<4.0.0` in pyproject.toml
- Dockerfile changed to install `.[dev]` so pytest is available in container

### Next steps
- Manual browser smoke test: open http://localhost:3000, log in, try chat with Assistant and Tom
- Write implementation plan for Sub-project 2: Quote Agent
- After Sub-project 2: Sub-project 3 (Mobile Chat Interface)
