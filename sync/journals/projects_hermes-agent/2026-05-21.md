# Work Journal: 2026-05-21

## Session 00:00
### What was done (prior session recap)
- Fixed Railway deployment: dashboard now accessible at https://hermes-auto-parts-production.up.railway.app/
- WhatsApp paired by running bridge locally with `--pair-only`, extracting creds.json, base64-encoding it, and setting as `WHATSAPP_CREDS_B64` env var
- Fixed gateway crash "Refusing to run as root" by setting `HERMES_ALLOW_ROOT_GATEWAY=1`
- Fixed config bootstrap order bug (auto-parts-entrypoint.sh now always overwrites config.yaml)
- Fixed WHATSAPP_CREDS_B64 too-large issue by only archiving creds.json (not all session files)
- Enabled bot mode: set `WHATSAPP_MODE=bot` + `WHATSAPP_ALLOWED_USERS=*` (bridge) + `GATEWAY_ALLOW_ALL_USERS=true` (gateway)
- Created `docker/railway-start.sh` as explicit startup script since Railway startCommand replaces ENTRYPOINT

### Current state
- All three goals achieved: dashboard accessible, WhatsApp connected, bot mode enabled
- Changes on branch `feat/automotive-profile` of fork `duranbrook/hermes-agent`
- No PR to upstream NousResearch/hermes-agent (these are deployment-specific changes)
- User asked: "Is there any PR I need to push?"

### Next steps
- Answer PR question: changes are self-contained in the fork, user can merge feat/automotive-profile → main on their fork if they want to clean up
