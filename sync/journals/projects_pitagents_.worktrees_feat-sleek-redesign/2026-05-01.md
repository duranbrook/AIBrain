# Work Journal: 2026-05-01

## Session 13:00

### What was done
- Implemented Task 1: CSS Tokens + Glass Utilities
- Modified `web/app/globals.css`:
  - Added `--accent-glow` token: `rgba(217, 119, 6, 0.36)`
  - Added `--bg-overlay` token: `rgba(0, 0, 0, 0.62)` with moody default
  - Removed `@media (prefers-color-scheme: dark)` media query (simplified to light theme baseline)
  - Added `.glass-panel` utility class: frosted glass with 28px blur, dark background, subtle border
  - Added `.glass-card` utility class: lighter glass for inner cards within panels
- Verified TypeScript build passes (1323ms compilation, no errors)
- Committed: `0e45af9 feat(web): add bg-overlay CSS token and glass panel utilities`

### Current state
- Task 1 complete and verified
- Build passing
- Ready for Task 2 (AppBackground component)

### Next steps
- Task 2: Create AppBackground component with gradient background + overlay

### Key decisions
- Removed dark mode media query to simplify initial design — aligns with the "sleek redesign" focus on light theme baseline
- Glass utilities use raw RGBA values for maximum flexibility with Tailwind v4

---

## Session 13:32

### What was done
- Implemented Task 2: Theme Hook + Avatar Utility
- Created `web/hooks/useTheme.ts`:
  - Exports `BgTheme` type (dark | moody | vivid) with overlay opacity mapping
  - `useTheme()` hook initializes from localStorage, applies `--bg-overlay` CSS variable to `:root`
  - `setTheme()` persists to localStorage and updates overlay in real-time without React re-render
- Created `web/lib/avatar.ts`:
  - `pravatarUrl()` utility generates consistent Pravatar URLs from any string ID
  - Maps first char code point to Pravatar's 1–70 image range via modulo math
- Verified TypeScript build passes (1443ms, no errors)
- Committed: `ccc55f3 feat(web): add useTheme hook and pravatarUrl utility`

### Current state
- Task 2 complete and verified
- Build passing
- Ready for Task 3 (AppBackground component)

### Next steps
- Task 3: Create AppBackground component with gradient background + overlay
