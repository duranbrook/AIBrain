# Work Journal: 2026-03-26

## Session 01:00 — Research & System Design
### What was done
- Fetched Finch Legal's website and documented their business model
- Researched pre-litigation process from 8 sources
- Created full system design document with Mermaid diagrams → `docs/system-design.md`
- Research docs: `docs/finch-legal-overview.md`, `docs/pre-litigation-process.md`, `docs/finch-temporal-blog-summary.md`, `docs/research-document-management.md`, `docs/research-legal-search.md`

---

## Session 10:00 — System Design Gaps
### What was done
- Self-reviewed system design and closed 8 gaps (sections 11-18)
- Added: data model, API design, auth/RBAC, HIPAA compliance, error handling, observability, CMS integration, cost estimation
- Rendered Mermaid diagrams as PNG images

---

## Session 11:00 — Kotlin Backend Implementation
### What was done
- Full Kotlin/Spring Boot backend: 62 files across domain layer
- All 12 controllers, 11 services, 14 JPA repositories
- Temporal workflow definitions (case lifecycle + document processing)
- Flyway migrations V1-V21 (18 tables)
- Multi-tenancy with RLS, JWT auth with RBAC, rate limiting

---

## Session 13:00 — Integration Tests & Frontend
### What was done
- Integration tests with TestContainers (cases, auth, RBAC, multi-tenancy, documents, notes, users)
- README with setup instructions
- Started React frontend build

---

## Session 14:00 — React Frontend Complete
### What was done
- 30+ TypeScript/React files: API layer (9 modules), auth, layout, 6 pages, 13 components
- 8-tab case detail view, dashboard, search, notifications
- Build passes with zero errors

---

## Session 15:00-21:00 — Feature Expansion (51 commits)
### What was done

**Backend Features (34):**
- CORS config, bug fixes (JSONB types, seed passwords, inet column)
- SOL tracking: all 50 states, auto-calculation, alerts, rules endpoints
- Treatment gap monitoring: per-case and org-wide alerts
- Dashboard analytics: pipeline, stalled cases, financials
- Settlement calculator: multiplier-based damages, attorney fees
- Disbursement calculator: settlement distribution with lien deductions
- Medical provider CRUD, lien tracking (Letters of Protection)
- Case timeline from audit logs, case status history tracking
- Case financials summary per-case
- Case access control (grant/revoke), case links (related cases)
- Case tags, case communication log, case duplicate detection
- Case assignment with auto-assign load balancing
- Case report (full summary), bulk status update
- Public intake endpoint (no auth, lead capture)
- Webhook system (HMAC-SHA256 signed, delivery log)
- API key management (create/revoke/hash)
- Task automation (16 PI-specific templates, auto-create on phase change)
- Document templates (LOR, demand letter, medical records request, settlement release)
- Client portal endpoints (simplified views)
- CSV export (cases, medical records)
- Rate limiting per API key
- Email notification service (SMTP/log-only) with user preferences
- Priority queue with urgency scoring
- Firm-level metrics endpoint
- Notes search with privileged access control
- Enhanced OpenAPI docs (14 tags)

**Frontend Features (8):**
- Analytics dashboard (pipeline chart, SOL/treatment gap alerts, financials)
- Settlement calculator UI in demands tab
- Real case timeline (API-backed with icons)
- New case form with state jurisdiction dropdown
- SOL badge on case detail header
- Case financials panel in overview
- Medical records with lien badges
- Communication log tab (9th tab)

**Quality:**
- Evaluator agent tested all 47 endpoints → found 7 bugs → all fixed
- 18 integration tests
- Comprehensive seed data (9+ cases, tasks, notes, demands, insurance)

**Infrastructure:**
- Dockerfile (multi-stage, non-root)
- Simplified startup (gradle.properties + run.sh)
- 1,135-line updated system design doc

**Docs & Process:**
- Consolidated work journal (13 files → 2, one per day)
- Updated `~/.claude/CLAUDE.md` with one-file-per-day journal format
- Learned and saved Anthropic's multi-agent harness patterns (generator/evaluator separation, sprint contracts)
- Deep research doc on PI CMS landscape → `docs/pre-litigation-research.md`

### Current state
- **51 commits** this session, **59 total**
- **95 REST endpoints**, **135 Kotlin files**, **45 frontend files**, **35 migrations**
- Backend: `./gradlew :app:bootRun` (port 8080)
- Frontend: `cd frontend && npm run dev` (port 5173)
- Swagger: http://localhost:8080/swagger-ui/index.html
- Login: `admin@acmelaw.com` / `password123`

### Key decisions
- Used evaluator agent pattern (from Anthropic blog) to test all endpoints — found 7 bugs I wouldn't have caught
- SOL rules seeded for all 50 states for motor_vehicle_accident; selective for premises_liability and medical_malpractice
- Treatment gap threshold defaults to 30 days (PI industry standard)
- Settlement multiplier auto-calculated 1.5x-5x based on medical bill amount
- Task automation triggers on case status change, not phase change (status is the source of truth)
- Webhook payloads signed with HMAC-SHA256 (Stripe-style security)
- API keys use SHA-256 hashing; raw key shown only once on creation

### Next steps
- E2E tests with Playwright
- Real S3 document upload integration
- WebSocket real-time notifications
- Mobile-responsive frontend
- Production deployment to Railway/Render

---

## Session 23:00 — E2E Browser Tests with dev-browser
### What was done
- Installed and configured dev-browser for headless browser automation
- Fixed frontend-backend API mismatch in case creation:
  - Frontend was sending `name`/`role` but backend expects `party_type`/`first_name`/`last_name`
  - Frontend used `plaintiff`/`defendant` roles but DB constraint requires `client`/`opposing`/`witness`/`expert`/`adjuster`
  - Updated `NewCasePage.tsx` to properly map form data to API format
  - Updated `PartyList.tsx` to render the new field names
  - Updated `cases.ts` API types to match backend DTOs
- Created comprehensive E2E test suite in `e2e/` directory (37 tests, all passing):
  - `01-auth.js` — Login, session, sign out, invalid credentials (6 tests)
  - `02-dashboard.js` — Stats, pipeline, recent cases table (6 tests)
  - `03-case-crud.js` — Case list, create, detail, seeded data (9 tests)
  - `04-search.js` — Search page, queries, empty state (5 tests)
  - `05-notifications.js` — Badge, page, navigation (4 tests)
  - `06-navigation.js` — Sidebar links, branding, routing (7 tests)
  - `run-all.sh` — Runner script with prerequisite checks

### Current state
- All 37 E2E tests passing in headless mode
- Case creation now works end-to-end (frontend → API → DB → redirect to detail)
- Tests are re-runnable: `./e2e/run-all.sh` or `./e2e/run-all.sh --headed`

### Key decisions
- Used dev-browser (Playwright-backed) instead of Cypress — simpler setup, JS scripts, no npm test framework needed
- Each test file is self-contained with its own login — avoids shared state issues between suites
- Fixed the party type mismatch rather than working around it in tests — the bug was real
