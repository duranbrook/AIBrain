# Work Journal: 2026-03-27

## Session 01:30 — Overnight Backlog Sprint
### What was done
- **#8 Case Timeline Phase Colors**: Green checkmarks for completed, blue ring for current, gray for future. Falls back to case status when Temporal workflow not running.
- **#13 GlobalExceptionHandler**: Added handlers for HttpMessageNotReadableException (400) and IllegalArgumentException (400) — was falling through to 500.
- **#14 S3 Test Config**: Created TestS3Config with mock S3Presigner for test profile.
- **#15 Fix Tests**: Fixed 9 failing backend tests — 401→403 assertions, seed-data-aware counts, unique emails, fresh case creation in tests.
- **#11 Communication Log**: Already fully implemented — verified working.
- **#12 Note Search**: Added Notes tab to search page with privilege badge display.
- **#3 Temporal Workflow Visualization**: Start workflow button, phase advance signals, MMI reached, demand approved, settlement amount, Temporal UI link.
- **#6 Analytics Page**: New page with firm metrics (settlement stats, cases by type/status/state, monthly intake), priority queue with urgency scoring, CSV export buttons. Added to sidebar.
- **#5 Frontend Wiring (11 features)**: Insurance claim creation form, medical record creation form, delete case button, case linking, document template generation, task templates, duplicate detection on NewCasePage, user workload on dashboard.
- **#10 Admin Page**: Webhooks tab (CRUD + delivery logs), API Keys tab (create/revoke with scopes/expiration).
- **Snake_case fix**: Fixed runtime crash from camelCase interfaces (CaseLink, DuplicateCandidate, UserWorkload) not matching backend JSON.

### Current state
- 48 E2E tests passing (7 suites)
- 49 backend tests passing
- 12 of 15 backlog items completed
- 4 commits pushed to main

### Additional work (continued session)
- **Multi-part upload**: S3 multipart for files >10MB, 5MB chunks, 3 concurrent uploads
- **Client Portal**: case list, detail, documents, messages with compose form
- **Remaining frontend wiring**: medical providers, case access control, disbursement calculator, bulk status update
- **Backend API fixes**: document download presigned GET URLs, email sending wired to EmailSender, template placeholders replaced with real org name
- **User profile page**: account info display, password change with validation
- **Search typeahead**: autocomplete dropdown with 300ms debounce
- **Case status update**: inline dropdown on case detail header
- **SOL alerts**: color-coded (red ≤30d, amber ≤90d) on dashboard
- **Keyboard shortcut**: press `/` for search from any page
- **Inline title editing**: click case title to edit, Enter/blur to save
- **E2E tests expanded**: 71 tests across 12 suites (added analytics, admin, portal, case features, profile)

### Final state
- 71 E2E tests passing (12 suites)
- 49 backend tests passing
- 15 of 15 original backlog items completed (except data lake architecture — design-only)
- 15 commits pushed to main
- ~5,000+ lines added

### Role-Based UI Access Control (user request)
- Built 3-layer permission system: permissions.ts → RoleRoute → component guards
- 30+ granular permissions for 4 roles (firm_admin, attorney, paralegal, client)
- Sidebar, routes, tabs, buttons all role-aware
- 9 RBAC E2E tests verifying admin sees all, paralegal restricted, redirects work

### Final stats
- 80 E2E tests (13 suites), 49 backend tests — all passing
- 20+ commits, ~8,000+ lines added overnight
- Features implemented: S3 upload, bulk upload, multipart upload, analytics, admin, portal,
  user management, profile, intake form, RBAC, case assignment/tags, notifications enhancement,
  search typeahead, inline editing, print support, case filtering/sorting, and more

### Remaining backlog
- #7 Long-Running Task Processing & Data Lake (design/architecture — needs discussion)
