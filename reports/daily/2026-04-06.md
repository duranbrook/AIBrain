# Daily Report: 2026-04-06

## Key Learnings (Top 3)

1. **Next.js 16 + Prisma 7 both have significant breaking changes** — Next.js 16 renamed `middleware.ts` → `proxy.ts` (and `export function middleware` → `export function proxy`). Prisma 7 removed `url` from schema; DB config moves to `prisma.config.ts` with `@prisma/adapter-pg` in the client constructor. These are silent failures — builds pass until runtime. Reference: `sync/journals/business/2026-04-06.md`

2. **Dual-mode auth pattern (cookies + Bearer JWT) cleanly supports web and mobile from a single backend** — `getSession()` accepts an optional `bearerToken` param; if present, it bypasses cookie lookup. Mobile uses `expo-secure-store` + Bearer headers; web uses httpOnly cookies. No separate auth service needed. Reference: `sync/journals/business/2026-04-06.md`

3. **Admin bootstrap pattern: allow first promote call when `adminCount === 0`** — avoids the chicken-and-egg problem (can't promote anyone to admin if there are no admins to grant the permission). No manual DB seeding required. Reference: `sync/journals/business/2026-04-06.md`

---

## Executed Today
*(to be filled in by the 9am execute task)*

## Blocked Items
*(to be filled in by the 9am execute task)*

---

## Context: AI Business Platform Build (buildco)

Today Joe ran a major build session producing a full-stack AI business platform in 4 relentless cycles:
- **Cycle 1**: Next.js 16 + Prisma 7 scaffold, 35 employee roles, Claude streaming chat, all API routes, landing + onboarding + dashboard
- **Cycle 2**: Daily Briefing service + popover component
- **Cycle 3**: Legal intake 3-step form, legal workflow API, POST /api/employees/hire
- **Cycle 4 (continuation)**: Auth (JWT, sign-up/login/logout), Admin dashboard (`/admin`), Expo mobile app (`buildco-mobile`)

Build is clean: 17 API routes, 0 TypeScript errors, live Postgres + Claude API verified working.

---

## Tomorrow's Tentative Plan

**Priority 1 (Doing Today):**
- [ ] **Interview rubric: refine promo platform question** — carry over from 2026-04-05
- [ ] **RAG deep dive: PartFinder implementation** — architecture complete, apply to car-parts codebase (carry over from 2026-04-05)

**Priority 2 (This Week):**
- [ ] **Configure Cowork with useful skills and connectors**
- [ ] **buildco: Phase 2** — task management API, daily briefing push notifications, brand kit generation (if Joe continues this project)
