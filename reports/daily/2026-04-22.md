# Daily Report: 2026-04-22

## Key Learnings (Top 3)

1. **New project `interview_stlab` is active** — After a 9-day quiet period, Joe started a CMDB/IT asset management app using a Postgres + Redis + MinIO stack. The 2026-04-21 session analyzed input data format differences (CSV vs JSON vs YAML for the same hardware inventory), and the 2026-04-22 session included significant pipeline work. This confirms the quiet period was real inactivity, not a sync failure.

2. **Multi-format ingestion pipeline pattern** — The stlab ingestion pipeline handles CSV/JSON/YAML hardware data. Notable fixes committed: `extra_fields` handling, `normalize_batch` guard (prevents partial ingestion on validation failure), and CI `update_path` fix. The normalize_batch guard is a useful defensive pattern worth reusing in any ingestion system.

3. **CMDB data model: user_groups + user_app_llm** — New migrations added group-based access control (`user_groups`) and per-user LLM assignment (`user_app_llm`). Relationship gap reporting is a key feature, suggesting the system tracks completeness of asset relationships. Groups tab design was also in scope today.

## Executed Today
(morning PPRE only — to be updated by 8pm retro)

## Blocked Items
Nothing currently blocked.

## Tomorrow's Tentative Plan
- Continue stlab work (active project)
- Revisit car-parts enrichment priority — consider demoting to P2 given stlab momentum
