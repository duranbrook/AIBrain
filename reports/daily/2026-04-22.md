# Daily Report: 2026-04-22 (Wednesday)

> Re-written by the 2026-04-23 morning plan after the overnight sync pulled in a late-night `projects_pitagents` session (23:09) that ran after the 20:00 evening retro was finalized. The pitagents thread is a new-ish active project (first-stack bring-up + auth flow) and it adds a third meaningful learning to the day, so this version of the retro supersedes the 20:00 version.

## Key Learnings (Top 3)

1. **Nuke-then-reingest is the correct reset after a pipeline shape-change.** After the morning's wave of stlab ingestion fixes landed (code-fence stripping, NullPool/Celery, recursive parser, `on_conflict_do_nothing` upsert, model alias), the very first action of the next stlab session was `/nuke-test-data` — Postgres truncate + Redis flush + MinIO clear, scripted as a single command. That's the right pattern when idempotency isn't yet proven end-to-end: don't patch partial state, wipe and replay from zero. The generalizable rule: **if a pipeline changed shape, the next run starts from empty, not from stale partials.** Having a one-shot reset command (vs. ad-hoc cleanup) is what makes this cheap enough to do routinely. *Source: `sync/journals/interview_stlab/2026-04-22.md`.*

2. **An 11-day carry-over P1 is a signal to demote, not to push harder.** The 09:00 execute run formally moved car-parts contact enrichment from P1 to P2 after 11 consecutive zero-progress days. The trigger wasn't new information about the task — the task hadn't changed — it was the *arrival of genuine momentum in a different project* (stlab). Backlog hygiene beats backlog sentimentality: **when a task has been P1 for ≥2 weeks with no commits and there's active momentum elsewhere, demote on that signal alone.** Re-scope before promoting again (here: top-10 chains as validation chunk, not the full 3,623). *Source: `tasks/backlog.md`, commits `3825f5f` + `11a09cb`.*

3. **First Docker-Compose bring-up of a new-ish project produces a predictable bundle of 3–5 config/dep/CORS bugs — budget for them, don't treat them as a blocker.** The pitagents 23:09 session brought the full stack (db, redis, backend, web) up from cold and hit five distinct startup bugs in sequence: `pydantic_settings` rejecting unknown env vars (fix: `extra="ignore"`), `passlib 1.7.x` vs `bcrypt 4.x` incompatibility (fix: pin `bcrypt<4.0`), missing `pydantic[email]` extras, default Next.js landing page instead of a `/dashboard` redirect, and missing `CORSMiddleware` blocking every browser→API call. None are deep — but they're the same five-ish bugs every Python+Next.js stack hits on a cold first start, and seeing them bundled turns "this is broken" into "this is the standard bring-up cost." **Rule: when bringing up a new stack, expect the config/auth/CORS tax and route around it quickly rather than treating each bug as a surprise.** *Source: `sync/journals/projects_pitagents/2026-04-22.md`.*

## Executed Today

- [x] **AIBrain morning plan (08:00)** — pulled latest, rewrote 2026-04-21 retro with 3 stlab learnings, updated backlog (stlab P1, car-parts demotion candidate), posted briefing. Commit `3825f5f`.
- [x] **AIBrain execute (09:00)** — committed stlab pipeline fixes + test repairs + migrations 0003/0004/0006 to stlab/main (4 commits); formally demoted car-parts to P2. Commit `11a09cb`.
- [x] **stlab: CMDB ingestion pipeline + test suite repair** — code fence stripping in classifier/relationship resolver, NullPool for Celery workers, recursive entity parser, `on_conflict_do_nothing` upsert for DeviceUserAssignment, model alias fix; 4 stale tests repaired; 80/80 unit tests passing. (Core work completed 2026-04-21 late / 2026-04-22 early; surfaced + committed by today's execute run.)
- [x] **stlab: session 09:00** — `/nuke-test-data` to reset CMDB (DB truncate, Redis flush, MinIO clear) as a clean base to validate the morning's pipeline fixes. Short research-phase session, no further commits.
- [x] **pitagents: session 09:00** — repository structure exploration + root-level README.md with quick-start guide. (Missed by 20:00 evening retro — pitagents wasn't in scope at the time of first retro drafting.)
- [x] **pitagents: session 23:09** — **full Docker Compose stack up** (db, redis, backend, web), **5 startup bugs fixed** (pydantic_settings, bcrypt/passlib, pydantic[email], `/` → `/dashboard` redirect, CORSMiddleware), **auth flow wired** (`/login` page with JWT in localStorage, dashboard auth guard). End state: test creds `owner@shop.com` / `testpass` work; dashboard loads reports from DB. (Happened after 20:00 — missed by evening retro, surfaced in overnight sync.)
- [x] **AIBrain evening retro (20:00)** — superseded by this re-write.
- [-] **car-parts: Contact enrichment** — demoted to P2 (day 11 stall, no external block). Re-scope to top-10 chains when revisited.

## Blocked Items

Nothing blocked. Two active threads now (stlab + pitagents); neither has an external dependency.

## Retrospective

Three meta-observations:

- **The 20:00 evening retro was incomplete because pitagents ran late-night.** Not a process bug — the evening retro has to run at *some* time, and a session that starts at 23:09 will miss any retro that ran earlier. The morning plan is exactly the catch-net for this, and it worked as designed.

- **Backlog is now two active projects, not one.** At 20:00 yesterday the shape was "one clear active project (stlab), everything else parked." That was accurate at 20:00 but untrue by 23:09: pitagents became the second active thread that night, and today's (2026-04-23) pitagents journal shows 27 commits on `feat/web-chat-ui` and Sub-project 1 complete. Today's plan must reflect two P1 threads, not one.

- **"Was today productive?" is the wrong frame for AIBrain itself.** AIBrain executed four scheduled steps cleanly (morning plan, execute, evening retro, sync) and made one correct priority call (car-parts demotion). That's the whole job. The stlab project had a quiet day (one session, no commits); the pitagents project had two productive sessions — facts about those projects, not about AIBrain.

## Carrying Into 2026-04-23

1. **stlab: CMDB ingestion + data model** — P1, active. Next: validate pipeline fixes against freshly nuked data; check 80/80 pass rate holds against re-ingested fixtures; continue relationship gap reporting + groups tab.
2. **pitagents: Web Chat UI + follow-ups** — P1, very active. Sub-project 1 (15-task plan) wrapped on 2026-04-23 morning per journal; next is manual browser smoke test, then Sub-project 2 (Quote Agent) plan, then Sub-project 3 (Mobile Chat).
3. **car-parts: Contact enrichment** — P2, re-scoped to top-10 chains when revisited.
4. **buildco Phase 2** — P2. Task management API + iOS push notifications if stlab / pitagents validation wraps early.
5. **Configure Cowork skills/connectors** — P2.
6. **Skills vs Connectors vs MCP** — P2 reference notes.
7. **Confirm sync pipeline health** — P3 (monitor — healthy today; overnight sync correctly caught the late-night pitagents session).
