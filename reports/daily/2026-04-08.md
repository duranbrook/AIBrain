# Daily Report: 2026-04-08

## Key Learnings (Top 3)

1. **Next.js 16 renames `middleware.ts` → `proxy.ts` (and its export)** — The file must be `src/proxy.ts` exporting `export function proxy`. The old name still works but causes a deprecation warning and appears broken in build output. This is not in training data and caused real build failures. Also: `npx tsc` is broken in Next.js 16 — use `node node_modules/typescript/bin/tsc --noEmit` instead.

2. **Prisma 7 removes `url` from `schema.prisma`** — Database URL now lives in `prisma.config.ts` under `datasource.url`. The constructor requires an explicit adapter: `new PrismaClient({ adapter: new PrismaPg({ connectionString }) })`. No `index.ts` is auto-generated — must create `src/generated/prisma/index.ts` manually with re-exports. These are silent runtime failures if you miss them.

3. **SwiftUI `navigationDestination(item:)` requires `Hashable` conformance** — Any enum used with `navigationDestination(item:)` must conform to both `Identifiable` AND `Hashable`. The compiler error doesn't directly name the missing conformance, making it confusing to diagnose. Source: `LoginView.LoginDestination` error in buildco-ios.

## Executed Today (updated by 9am run)
- [x] **buildco: E2E smoke test** — All 4 flows verified: sign-up, onboard, dashboard, employee chat (CEO Agent + Fullstack Engineer). 5 employees auto-created on onboard. Streaming SSE chat returns live tokens from Claude. Messages persisted to DB. All pages return HTTP 200.

## Blocked Items
- None — Priority 1 complete.

## Key Finding: ANTHROPIC_API_KEY needs explicit env export for production
When starting `next start` (production mode), `ANTHROPIC_API_KEY` must be in the shell environment (not just `.env`) because the Anthropic client is instantiated at module load time. Run: `export $(cat .env | grep -v '#' | xargs) && node node_modules/next/dist/bin/next start`. Note: Next.js binary (`node_modules/.bin/next`) is broken in Next.js 16 — use `node node_modules/next/dist/bin/next` directly.

## Smoke Test Results
| Step | Status | Details |
|------|--------|---------|
| Sign Up (POST /api/auth/signup) | ✅ PASS | JWT returned, user created in DB |
| Onboard (POST /api/onboard) | ✅ PASS | Business + 5 employees created, userId linked via session cookie |
| Dashboard (GET /api/business/:id) | ✅ PASS | Returns business + employees |
| CEO Agent chat (POST /api/chat) | ✅ PASS | SSE streaming works, messages persisted |
| Engineer chat (POST /api/chat) | ✅ PASS | Separate employee conversation works |
| Pages (/signup, /onboard, /dashboard) | ✅ PASS | All return HTTP 200 |
| DB migrations | ✅ PASS | Already applied, schema up to date |

## Tomorrow's Tentative Plan
- **buildco: E2E smoke test** — Set `ANTHROPIC_API_KEY` + `DATABASE_URL`, run `prisma migrate dev --name init`, test sign-up → onboard → dashboard → employee chat flow
- If smoke test passes: begin **buildco Phase 2** — task management API + daily briefing push notifications
