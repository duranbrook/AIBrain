# Daily Report: 2026-05-01

> **Note on cycle state:** Today's pipeline ran the sync (00:15) and execute (00:20) phases but no daily report was written before this evening retro. The last `chore(plan)` / `chore(execute)` commits in `git log` are dated 2026-04-28; today's execute-run edits to `tasks/backlog.md` were left uncommitted (mark P2 InspectScreen reconcile + RecordingActivity.BASE_URL fix as done). The last evening retro was 2026-04-27. **This report does double duty as the evening retro for 2026-05-01 and the catch-up retro for 2026-04-29 and 2026-04-30** (which both lack daily reports). Learnings below are sourced from the just-synced journals — `sync/journals/projects_pitagents/2026-04-30.md` is the substantive content; 2026-04-29 was effectively a no-op day; 2026-05-01 was sync + a small read-only QA + 1 sandbox-local commit on a separate repo.

## Today's Top 3 Learnings (2026-05-01) — finalized by 20:00 evening retro

These are the three most significant *new* learnings from today's actual work (the sleek redesign + dashboard pipelines that shipped to `pitagents.vercel.app` during the morning, and the AIBrain cycle-collision pattern that surfaced overnight). They supersede the bonus list flagged in the midday update; the canonical 04-30 top-3 below is preserved unchanged because this report still doubles as the retro for that day.

1. **Next.js client/server boundary trap: storing React component functions inside a "data" object silently forces every consumer of that object client-side.** In `web/components/dashboard/tiles.tsx`, each `GROUPS[i].tiles[j].icon` field stores a React component function (`MyIconComponent`). The whole `GROUPS` constant *looks* like plain JSON data — and is otherwise serializable — but a component-function reference cannot cross the Next.js server/client boundary. The pragmatic fix Joe landed: keep `tiles.tsx` itself unmarked (so it stays server-renderable as data), and let only the consumers that actually invoke `<icon />` (`FeatureTile`, `GroupSection`) be marked `'use client'`. Generalizes: anything you store in a "config" object that ends up rendered as JSX is a hidden client-boundary tripwire — keep the *invocation* on the client side, not the *registration*. Source: `sync/journals/projects_pitagents/2026-05-01.md` "dashboard implementation".

2. **Double-submit prevention in async login: a `navigated` flag set in the success branch and consumed in the `finally` block is cleaner than disabling the form button.** Sleek-redesign's Login fix: `try { await login(); navigated = true; router.push(...) } finally { if (!navigated) setSubmitting(false) }`. The standard pattern of disabling the submit button immediately after first click flickers when the navigation race resolves and re-enables the disabled state in `finally`. The `navigated`-flag pattern moves the gating into the finally block itself: if we successfully kicked off navigation, we never re-enable. Generalizes to any `try { side-effectful-thing-that-changes-route(); navigated = true } finally { ... }` — one boolean, no extra state, no flicker. Source: `sync/journals/projects_pitagents_.worktrees_feat-sleek-redesign/2026-05-01.md` Task 6.

3. **Concurrent-session collision on `.git/*.lock` is the failure mode when AIBrain's scheduled tasks overlap.** Today the morning-plan and evening-retro fired simultaneously (both were delayed by the cycle-scheduler gap). They raced on `.git/*.lock` files held in read-mode by macOS Xcode's `com.apple.dt.Xcode.sourcecontrol.Git.xpc` (PID 7957). `lsof` showed all four lockfiles held open. Recovery pattern: rename (don't `rm -f`) the locks to `.lock.stale.<ts>` — recoverable forensics, both racing sessions then drain. Sandbox cannot delete files inside the fuse-mounted `.git/` directory, only rename, so `mv` is the *only* clean tool here. Operational consequence: if multiple scheduled tasks are likely to overlap, gate each cycle script behind a `lsof | grep -q '.git/.*\.lock'` pre-check and skip on contention rather than racing. Source: `sync/journals/AIBrain/2026-05-01.md` Session 00:30.

## Carryover Learnings (canonical for 2026-04-30 — retained from morning retro)

1. **In a hold-to-talk button over WebRTC, browser event order — `mousedown → mouseup → click` — will cancel the model's tool response unless you guard the click handler.** Joe's 2026-04-30 16:00 session diagnosed why the voice control showed "connected" (green ring) but never navigated: the natural sequence in PTT mode is `mousedown` → `startCapture()` (audio track on), `mouseup` → `stopCapture()` (audio committed to OpenAI), `click` → `disconnect()` — and the click fires *before* OpenAI sends back the tool call, killing the session early. Fix: a `mouseDownAt` ref to track hold duration; holds > 200ms suppress the click-handler's `disconnect()`, while short taps still toggle off intentionally. Generalizes to any "hold-to-X, tap-to-Y" affordance over an async backend: don't put the X-completion handler on `click`, and if you must, gate it on a duration measured from `mousedown`. Source: `sync/journals/projects_pitagents/2026-04-30.md` Session 16:00.

2. **OpenAI Realtime tool chaining: `tool_choice: 'auto'` is correct, `'required'` traps you in the depth limit, and `responseId` from `response.output_item.done` is unreliable for chain-state tracking.** Joe's 2026-04-30 final session diagnosed why a multi-step voice command ("go to reports and find the Honda Civic") was firing tool 1 and then stalling: (a) the chain condition `(pendingCalls.length > 0 || executedCount > 0)` evaluated `executedCount=0` because `responseId` from `output_item.done` was sometimes null, so the `#responseToolCounts` map never got populated for that response — so `#requestPostToolResponse` never fired. Fix: add a session-scoped `toolExecutedDuringResponse` boolean, set inside `#executeToolCall`, reset on every `response.created` — a more reliable proxy for "a tool ran in this response" than counting by responseId. (b) `tool_choice: 'required'` was forcing the model to keep calling tools even when the chain was logically complete, burning through `depth=8`. `'auto'` lets the chain terminate naturally. (c) Updated INSTRUCTIONS to "call ALL needed tools in a single response" so the model batches in response 1 and stops on the post-tool `response.create`. Source: `sync/journals/projects_pitagents/2026-04-30.md` final session.

3. **A Next.js Vercel project with `framework: null` silently uses `@vercel/static-build` — only `public/` assets ship, and prod 404s on every dynamic route.** Joe's 2026-04-30 15:20 session: `pitagents.vercel.app` was returning 404 because the Vercel project had been created with `framework: null` instead of `nextjs`. Vercel's default builder for null-framework is `@vercel/static-build`, which deploys only static files (the SVGs in `public/`) — the Next.js bundle never goes up. *Plus* a second axis of failure: the team had been deploying to a different Vercel project (`web`) all along, not `pitagents`. Fix sequence: re-link `web/` → `pitagents` project, set `framework: nextjs` via Vercel API, pull settings, rebuild, deploy prebuilt 2.3MB artifact. Two takeaways for future Vercel-deploy hygiene: (a) always confirm `vercel link` points to the project the URL implies; (b) inspect `framework` in project settings — `null` is silently destructive for SSR apps. Source: `sync/journals/projects_pitagents/2026-04-30.md` Session 15:20.

## Executed Today (2026-05-01)

- [x] **pitagents web: Sleek redesign — feat/sleek-redesign merged to main (`da27f2e`)** — 9-task subagent-driven shop. (See full description below in Midday Update.)
- [x] **pitagents web: Dashboard / Home tab — feat/dashboard merged to main (`abddfc2`)** — 5-task subagent-driven shop. (See full description below in Midday Update.)
- [x] **AIBrain: scheduled `aibrain-evening-retro` ran at 20:00 (this run)** — Re-evaluated today's full journal, finalized today-specific top-3 learnings (Next.js client/server boundary trap, `navigated`-flag double-submit prevention, concurrent-session `.git/*.lock` collision pattern), refreshed Phase Notes. Backlog/blocked already current from the midday plan run.
- [x] **AIBrain: scheduled `aibrain-sync` ran at 00:15** — `bash scripts/sync.sh` pulled local journals + memory into the AIBrain repo. Commit `ba4df30 chore(sync): 2026-05-01 — sync journals and memory`. AIBrain `main` is now 2 commits ahead of origin (this commit + `6935fc9` from Tuesday).
- [x] **AIBrain: scheduled `aibrain-evening-retro` ran at 20:00** — this report. Catches up the 3-day gap since the last retro (2026-04-27 evening).
- [x] **AIBrain: scheduled `aibrain-execute` ran at 00:20** — Phase 4 task execution.
  - Phase 2 (owner-input): `tasks/owner-input.md` empty. The 2026-04-28 16:37 China-phone owner input is a pitagents-side action and stays in the journal.
  - Phase 4: P1 has 4 actionable items + 1 already-shipped (iOS `MessageResponse`, completed 04-28 09:03). All 4 remain owner-gated / live-data-gated; falling through to P2 per the up-to-2 rule.
  - **P2 [x] InspectScreen spec vs. implementation reconcile** (read-only QA): iOS `InspectTabView` + `CustomerVehiclesInspectView` and Android `InspectScreen` both implement the 2-step `customer → vehicle → recording` flow per `recording-inspect.feature`. Minor non-blocking divergence: iOS `LazyVGrid` matches "grid"; Android `LazyColumn` is a list — functionally equivalent for the navigation behavior the spec actually exercises. No code changes.
  - **P2 [x] `RecordingActivity.BASE_URL` Railway URL fix** — pointed `RecordingActivity.BASE_URL` from `http://10.0.2.2:8000` (AVD loopback) to `https://backend-production-5320.up.railway.app` so the inspection recording flow can reach the backend on a physical device. Mirrors the URL `ApiClient.kt` and `ReportDetailScreen.kt` already use. Commit `8b02e2b` on `pitagents/main` (sandbox cannot push to pitagents — awaits local cron sync).

## Executed Yesterday (2026-04-30) — captured here because no daily report exists

- [x] **pitagents web: Voice control layer (Sub-project A) shipped to production** — Vendored realtime-voice-component into `web/lib/voice/`, server-side `/api/session` route exchanging `OPENAI_API_KEY` for ephemeral WebRTC token, `VoiceContext` for cross-component callback refs, 5 tools (`navigate_to_tab`, `select_agent`, `send_message`, `select_customer`, `select_report`), `VoiceControlWidget` (mic button, status ring, PTT toggle), `VoiceProvider` in `providers.tsx`. Reports + Customers pages added `voice_select` URL-param auto-selection (Suspense wrapper required). Live at `https://pitagents.vercel.app`.
- [x] **pitagents web: Reports page rewrite (Sub-project B)** — Glass cards, severity icons (✕/⚠/✓), labor hours sub-line, accent colors, voice_select param. Mirrors the iOS `ReportDetailView`.
- [x] **pitagents backend: PATCH /reports/{id}/estimate live** — Editable estimate table on the Reports page. 5 tested cases pass (200 happy path, 422 negatives, 401 no-auth, 404 missing report, 422 missing fields); 403-non-owner not curl-testable due to seed data.
- [x] **pitagents code review fixes (8 issues)** — Spec compliance: `get_current_user` → `require_owner` on PATCH endpoint. Code quality: React updater purity (`patchAndSync` outside `setState`), blob URL leak, URL-safe base64 in `api.ts`, `ge=0` constraints on Pydantic model, `labor_rate` missing from quote-format branch of `_to_staff_detail`, `expire()` before `refresh()`, 403 test fixture.
- [x] **Production deploy fix: `pitagents` Vercel project framework misconfig** — see Learning 3.
- [x] **Production deploy fix: `lightningcss-darwin-arm64` removed from package.json** — macOS-only binary was breaking the Linux Vercel build.
- [x] **PTT voice navigation bug fix** — see Learning 1.
- [x] **OpenAI Realtime multi-step tool chain fix (3 commits, deployed)** — see Learning 2.

## Executed Wednesday (2026-04-29) — captured here

- [-] **No active development** — `sync/journals/projects_pitagents/2026-04-29.md` is 16 lines. One 09:00 session: a business-question conversation about LLC vs S-Corp vs C-Corp, no code touched. Web UI redesign and voice brainstorming happened *late evening* and continued into 2026-04-30 (see 04-30 Session 00:00 "Where we left off").

## Blocked Items (end-of-day)

- **pitagents: Push backend commits to Railway** — Status partly resolved by 2026-04-30 14:00: the deploy-and-test session ran `railway up --service backend` from Joe's laptop and verified PATCH /reports/{id}/estimate is live in prod. Still untracked: whether the older `a504cf3` (end-to-end report generation pipeline) and `6307b71` (Twilio resilience + InspectScreen) commits are now on Railway, or whether 04-30's deploy was a separate change set. **Needed to unblock fully:** Joe confirms by next morning whether the 04-30 Railway deploy included `a504cf3`/`6307b71`, or whether a follow-up `railway up` is still needed.
- **pitagents: iOS Simulator validation** — *Partly unblocked.* 04-30 noted "Navigation to customer detail confirmed via screenshot (automated tapping working)" — the cliclick-style regression from 2026-04-27 evidently has a working path again on iPhone 16 sim. Outstanding: deep navigation to the report-with-estimate flow not yet automated ("simulator tap coordinate calibration needed for secondary display"). **Needed to unblock:** Joe runs the estimate-edit flow manually on the sim, OR the agent recalibrates tap coords for the deep flow.
- **pitagents: Multi-step voice chain user-confirmation** — 04-30 final session shipped 3 commits with debug `console.log` statements deliberately left in place. Awaiting Joe to test "go to reports and find the Honda Civic" in browser, paste the `[voice] chain-check` console output. **Needed to unblock:** Joe runs the test; either confirm and clean up debug logs, or paste failure output for next-step diagnosis.
- **pitagents: Android E2E on simulator/device** — 04-30 noted "No AVDs configured on this machine — not tested in simulator." Gradle build was verified during feature implementation. **Needed to unblock:** AVD configured (or a connected device).
- **pitagents: Monitor real inspection results for per-finding photo matching quality** — Unchanged since 2026-04-27 retro. Needs live PDFs.
- **buildco Phase 2 kickoff** — Soft-blocked. Mobile smoke is now meaningfully advanced (Android emulator stand-in via Pixel 10a 04-27, iOS sim 04-30 confirmed login + navigation + estimate edit on web), so the original "E2E mobile smoke must pass" gate is approximately satisfied for the surface area BuildCo cares about.

## Carrying Over to Tomorrow (2026-05-02)

1. **pitagents: Confirm Railway deploy state** — verify whether `a504cf3` + `6307b71` shipped with the 04-30 backend deploy or whether they still need to be pushed.
2. **pitagents: User-validate multi-step voice chain** — Joe to test PTT chain and paste console output; on success, clean up debug `console.log` lines from `voiceControlController.ts` and related voice files.
3. **pitagents: Sub-project C — voice editing of report line items** — modify price/quantity via voice. Spec: not yet written.
4. **pitagents: iOS deep-navigation calibration for report estimate flow** — needed to fully validate the 04-30 ship.
5. **AIBrain: investigate why daily-cycle scheduler skipped daily reports for 2026-04-29 / 04-30 / 05-01** — sync + execute ran today (00:15 + 00:20) but no morning-plan and no `reports/daily/YYYY-MM-DD.md` was written by either; the same gap exists on 04-29 and 04-30. The morning-plan slot is the most likely missing piece in the scheduled-task config. P3 — schedule audit. *Filed as a new P3 backlog entry.*
6. **AIBrain: W18 weekly rollup** — due Sunday 2026-05-03.

## Phase Notes

- **Phase 1 (retrospective):** Done — top-3 finalized at 20:00 evening retro (today's actual learnings replace the 04-30 carryover at the head of the report; 04-30 retained as historical retro-of-record below).
- **Phase 5 (wrap-up):** Done at 20:00 — backlog/blocked already current from midday plan; this commit closes out the cycle.

## Midday Update (2026-05-01 ~12:00 UTC) — appended by delayed morning-plan run

The 11:54 UTC `aibrain-sync` (`4c7b529`) brought in journals showing Joe shipped two substantial pitagents web tracks during the morning that weren't in the original Phase 1 snapshot:

- [x] **pitagents web: Sleek redesign — feat/sleek-redesign merged to main (`da27f2e`)** — 9-task implementation via subagent-driven development. Glass-panel design tokens (`--accent-glow`, `--bg-overlay`, `.glass-panel`, `.glass-card`); `useTheme` hook with localStorage + `--bg-overlay` CSS-var live update; `pravatarUrl` avatar utility; `AppBackground` component (fixed photo + CSS-var overlay); `SettingsDropdown` (theme picker + sign out); full `AppShell` rewrite (frosted nav, SVG icons, Pravatar avatar); Login redesign with double-submit fix (`navigated` flag in finally block); Customers / Reports / Inspect / Chat AgentList all moved to glass panels with amber accent. All 11 routes compile cleanly; production build passes. Source: `sync/journals/projects_pitagents/2026-05-01.md` "sleek redesign completion" + `projects_pitagents_.worktrees_feat-sleek-redesign/2026-05-01.md` 13:00 + 13:32.
- [x] **pitagents web: Dashboard (Home tab) — feat/dashboard merged to main (`abddfc2`, fast-forward)** — 5-task implementation via subagent-driven development. `web/components/dashboard/tiles.tsx` with 6 group configs and 23 tiles (7 live / 16 coming-soon) + 23 inline SVG icon components; `FeatureTile` with live (Link + hover) vs. soon (inert div + badge) variants; `GroupSection` with coloured-dot header; `DashboardPage` mounted at root `/`; Home tab prepended to `AppShell` nav with corrected active-detection (`exact: true` for `/`, `pathname.startsWith()` for others). Key decision: dashboard components needed `'use client'` because `GROUPS.icon` stores React component functions (non-serializable across the Next.js server/client boundary), but `tiles.tsx` itself stays server-side as data. Source: `sync/journals/projects_pitagents/2026-05-01.md` "dashboard implementation".

### Bonus learnings (now superseded by the 20:00 evening retro top-3)

The midday update flagged three bonus learnings for tonight's retro to elevate. The retro promoted #1 (Next.js client/server boundary) and #2 (`navigated`-flag double-submit) into the canonical top-3 above, plus added a third — concurrent-session collision on `.git/*.lock` — which was the more operationally significant new pattern of the day. The third midday-flagged item ("subagent-driven development methodology has stabilized — 4th consecutive multi-task feature shipped in one day") is real but not new: it's a continuation of W17/W18's known SDD-as-default observation already captured in the April monthly rollup, so it didn't displace either of the truly new findings.

