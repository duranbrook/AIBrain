# Blocked Items

> **2026-05-05 20:00 EDT (Tuesday evening-retro):** No items moved between blocked / unblocked today — sandbox was blind to the entire EDT business day (no sync ran post-morning-plan; today's evening retro is the first un-rescued canonical-window stale fire). All 11 blockers carry forward unchanged. Tomorrow's 11:50 UTC sync will reveal whether any owner-side resolution happened during today's invisible window.
>
> **2026-05-05 12:00 UTC (Tuesday morning-plan):** No items moved in either direction overnight — no new project journals or memory entries synced (only meta AIBrain journal addendums). All 11 blockers from yesterday's evening retro carry forward unchanged. The voice-chain validation block (`User-validate multi-step voice chain + clean up debug logs`) now has a **strong candidate root-cause fix** in scope: Vercel `maxDuration = 60` for `/api/session` (shipped yesterday in `f241282`). On owner re-test, a green run would close the block outright. The iOS Stop button block is **narrowed to behavioral-only**: code is on iPhone SE as of yesterday's 08:29 EDT install; only "tap Stop mid-generation, confirm optimistic message disappears" remains. Table below is current as-is.
>
> **2026-05-04 20:00 UTC (Monday evening-retro):** **3 items resolved by today's pitagents ships** (technician system-prompt SQL → done via migrations 0029+0030; voice-transcription 413 root cause → fixed via `_strip_base64_images()` deployed yesterday evening; Railway-deploy state for migrations 0024–0028 → confirmed live via post-deploy verification on `/reports/{id}`). **2 new entries added:** iOS Stop button physical-device verification (today's iOS code change builds but cancellation behavior unverified on hardware); pitagents migration-chain verification before `feat/ios-technician-chat` merge (main now has 0023 → 0028 → 0029 → 0030 while branch has 0023 → 0024 → 0026 — sequencing question must be resolved at merge time). Net: 14 → 13 blocked.
>
> **2026-05-03 20:00 UTC (Sunday evening-retro):** No items moved between blocked / unblocked today. Nothing new to add. Table below is current as of this morning's plan run + this confirmation.

| Task | Blocked Since | Reason | Needed to Unblock |
|------|--------------|--------|-------------------|
| pitagents: Merge `feat/ios-technician-chat` into main (24+ commits ahead) | 2026-05-03 | Branch holds the iOS technician chat (15 tasks) + homepage/login/demo (9 tasks). Build succeeds, all 16 backend tests pass, TypeScript compiles cleanly. Hard prerequisite: owner must first complete the Google Cloud Console OAuth client ID setup so the homepage `/login` Google flow has a real audience to verify. Without that, merging would put a half-functional `/login` page into prod. **2026-05-04 update — also blocked on migration-chain reconciliation:** main now has 0023 → 0028 → 0029 → 0030 (shipped today); branch has 0023 → 0024 → 0026. The branch's migration files need to either be rebased on main's new head (renumbered to 0031/0032) or the chain conflict explicitly resolved. | (1) Owner creates a Google OAuth 2.0 Client ID in Google Cloud Console, sets `GOOGLE_CLIENT_ID` (Railway backend env) + `NEXT_PUBLIC_GOOGLE_CLIENT_ID` (Vercel + `.env.local`). (2) Migration chain reconciled: rebase branch on main, renumber migrations, verify `alembic history` is linear, retry build + tests. Then merge, push, Railway auto-deploys, Vercel auto-deploys (or `vercel --prod --force` from web/). |
| pitagents: Verify migration chain on `feat/ios-technician-chat` merge — *NEW 2026-05-04* | 2026-05-04 | Today's three direct-to-main pushes (0028, 0029, 0030) all extended the chain past 0023, but `feat/ios-technician-chat` already has 0024 + 0026 sitting unmerged. Two parallel histories exist; merging will hit a sequencing conflict. Sandbox can do the inspection diff but the resolution decision (rebase vs. squash vs. branch-rewrite) is owner-driven. | From Joe's laptop: `cd ~/workspace/projects/pitagents && git fetch origin && git diff origin/main feat/ios-technician-chat -- alembic/versions/` to see the divergent files; `alembic history` on each branch to see the chain shape; decide rebase or alternative; if rebasing, renumber 0024 → 0031 + 0026 → 0032 (or whatever follows 0030), update each file's `down_revision`, re-run tests. Sandbox can prep the rebase plan as a P1 sub-task tomorrow. |
| pitagents: Google Cloud Console OAuth client ID setup | 2026-05-03 | No way for sandbox to create a Google OAuth client — owner must visit console.cloud.google.com, configure consent screen, create OAuth 2.0 Client ID (web application), set authorized JavaScript origins + redirect URIs. | Owner creates the client ID and shares it (or sets the env vars directly). |
| pitagents: Test iOS Technician Chat on physical device — camera, VIN scanner, video recording | 2026-05-03 | Simulator can't exercise camera/microphone/video. Build is on iPhone SE (A0BA0276-F03D-5300-95C7-21F1360A6EB5) but the surface area was not exercised yet. *2026-05-04 narrowing:* voice transcription's underlying 413-error root cause was fixed by `_strip_base64_images()` shipped 05-03 evening + the device install completed. The remaining hardware-only surfaces are camera/VIN/video. | Owner runs the app on the physical device and exercises: `CameraCaptureView` multi-photo capture + thumbnail strip, `VINScannerView` viewfinder, `VideoRecorderView` + torch cancel. Reports back PASS/FAIL per surface. |
| pitagents: Verify iOS Stop button works on physical device — *NEW 2026-05-04* | 2026-05-04 | Today's iOS Stop button code change (`AgentChatViewModel._sendingTask` + `cancelSending()` + `AgentChatView.inputBar` red `stop.circle.fill` swap + `TechnicianInputBar` Send → Stop capsule) builds successfully, but the cancellation behavior — Task cancellation removing the optimistic message + UI swap timing — needs hardware verification. | Owner runs the technician chat on iPhone SE, sends a message, taps the red Stop button mid-generation, confirms the optimistic message disappears and the input bar reverts to Send. Repeat with image-attached send. |
| pitagents: Owner approval of 3-pillar homepage mockup before writing the spec | 2026-05-03 | Mockup served at `http://localhost:63942/three-pillars-mockup.html` (or file at `.superpowers/brainstorm/55140-1777773447/content/three-pillars-mockup.html`). Owner needs to make a design decision before code work continues. | Owner views the mockup, says approve / iterate / different direction. |
| pitagents: Owner follow-up on cut-off connected-ecosystem sentence (2026-05-02 21:30 vision input) | 2026-05-03 | Pillar 3 of the product vision was stated as "Vehicle History for consumers + Connected Ecosystem: any car's history is queryable — both shop and consumer side can pull it" with the next sentence cut off. Need the rest to scope Pillar 3. | Owner finishes the thought (e.g., what's the data model? who can query? what's the consent model?). |
| pitagents: Confirm Railway deployed `d293c5b` (autoshop-web migrations 0010–0021) + `0cb3a34` (migration 0022) + `1cea1e7` (seed UUID fix), and verify 0024 + 0026 once `feat/ios-technician-chat` merges | 2026-05-02 | Sandbox has no Railway credentials. 13 fresh migrations + a major schema expansion went out in two pushes during late-2026-05-01, plus 2 more migrations queued behind Saturday's branch merge. If any migration fails to apply, every new endpoint (job-cards, invoices, payments, inventory, vendors, time-tracking, accounting, diagnose, marketing, agents, technician-chat quotes-with-vehicle, demo-requests) returns 500 against prod. | From Joe's laptop: `railway logs --service backend \| head -200` (look for migration apply lines / rollback) and `railway run --service backend python -c "from alembic import command, config; command.current(config.Config('alembic.ini'))"` to confirm head matches local. If a migration failed, `railway up --service backend` to retry. |
| pitagents: Trigger seed_demo.py against Railway prod | 2026-05-02 | Sandbox can't reach Railway prod with `X-Admin-Secret`. `POST /admin/seed` is live, but the seed run wasn't triggered after the `1cea1e7` UUID fix shipped. Until run, the 7-customer demo data narrative doesn't exist in prod, so any demo / sales / UX validation against the live backend has no fixtures. | From Joe's laptop or any auth'd shell: `curl -H "X-Admin-Secret: demo-seed-2026" -X POST https://backend-production-5320.up.railway.app/admin/seed`. Then validate by browsing `/customers` in the live app and confirming the 7-customer narrative renders. |
| pitagents: Confirm Railway deploy state for `a504cf3` + `6307b71` | 2026-04-26 | Sandbox has no Railway credentials. `a504cf3` (report-generation pipeline, 2026-04-26) and `6307b71` (Twilio resilience + InspectScreen, 2026-04-27) may or may not have shipped with the 2026-04-30 14:00 `railway up --service backend` (which definitely shipped the PATCH /reports/{id}/estimate change set and was verified live). Largely subsumed by the 2026-05-02 Railway-state confirmation above (any prod logs query covers this too); kept separate because if those older commits aren't on Railway, prod still returns HTTP 500 on `POST /vehicles/{id}/messages` when Twilio is flaky. | From Joe's laptop: `git log origin/main..main -- backend/` plus `railway logs --service backend \| head` to confirm running commit SHA. If older commits not deployed, run `railway up --service backend` once more. |
| pitagents: iOS Simulator deep-navigation calibration for report estimate flow | 2026-04-30 | Sandbox cannot run Xcode. 2026-04-30 sim run cleared the cliclick regression for the upper flow (login → customer detail confirmed via screenshot), but deep navigation to the report-with-estimate edit screen "needed coordinate calibration for secondary display." | Joe either runs the estimate-edit flow manually on the sim, recalibrates tap coordinates, or switches the driver to `xcrun simctl io <UDID> input` + accessibility identifiers (more reproducible across rebuilds). |
| pitagents: User-validate multi-step voice chain + clean up debug logs | 2026-04-30 | 2026-04-30 final session shipped 3 commits with debug `console.log` statements deliberately left in. Sandbox cannot run a browser session against `pitagents.vercel.app` to validate the `[voice] chain-check` output. | Joe runs PTT with "go to reports and find the Honda Civic", pastes browser console output. On confirm: sandbox cleans up debug logs. On failure: paste output for diagnosis. |
| pitagents: Android E2E on simulator/device for 2026-04-30 voice + estimate-edit features | 2026-04-30 | "No AVDs configured on this machine — not tested in simulator." Gradle build verified during feature implementation, but the voice + estimate-edit surface area was not exercised on Android. | AVD configured (or a connected device — Pixel 10a from 2026-04-27 is a known-good baseline). |
| pitagents: Monitor real inspection results for per-finding photo matching quality | 2026-04-27 | Sandbox cannot observe production; verifying photo-to-finding matching needs eyes on actual generated PDFs from real inspections. | Joe shares 1–2 recent inspection PDFs (or the underlying `findings.photo_url` rows). Alternatively, a P2 task to add `logger.info` per-render counters gives the same signal from logs. |
| buildco Phase 2 kickoff | 2026-04-26 | Soft-blocked. Hard prerequisite was "E2E mobile smoke test passes." Substantially de-risked: Android Pixel 10a 2026-04-27 (5 PASS / 2 FAIL — both fails fixed in `6307b71`); iOS sim 2026-04-30 confirmed login + nav; web estimate-edit surface tested live. The original gate is approximately satisfied for the surface area BuildCo cares about. | Either iOS deep-navigation also clears, or Joe accepts ~80% confidence from the partial mobile smoke and greenlights buildco Phase 2 in parallel. |

## Recently Unblocked (2026-05-04)

- **pitagents: Run SQL to update existing Technician agent system prompts in DB** (was P1 / blocked since 2026-05-03). Resolved by migrations 0029 + 0030 deployed today: 0029 directly UPDATEs the Technician agent row to use the `report_builder` tools registry; 0030 directly UPDATEs the system prompt with the new PHOTO HANDLING section + numbered workflow steps. The DB-level UPDATE is a deployed migration, no manual SQL needed.
- **pitagents: Voice transcription on physical device** (was a sub-surface of the broader iOS technician-chat hardware-test block). 413 "request too large" root cause fixed by `_strip_base64_images()` in `chat.py` shipped 05-03 evening + iOS device install completed. The remaining iOS-hardware items (camera / VIN / video) are now narrower than yesterday's blocker.
- **pitagents: Confirm Railway deployed migrations 0024 / 0026 / 0028 / 0029 / 0030** (older variant blocked since 2026-05-02). Resolved live via post-deploy verification: today's three direct-to-main pushes all applied cleanly on Railway after the `00c883b5` SQL syntax fix; verification queries against `/reports/{id}` confirm Technician agent has the new tool list and system prompt; presigned URL flow returns HTTP 200. Migrations 0024 / 0026 still on the unmerged feat branch (now their own block above), but the rest of the chain past 0023 is confirmed live.

## Recently Unblocked (2026-05-03)

- **pitagents: feat/settings-panel — all 10 tasks shipped + merged to main** (was P1 since 2026-05-01 worktree open). All 21 commits merged 2026-05-02. No longer a blocker.

## Recently Unblocked (2026-05-01)

- **pitagents: iOS Simulator validation (upper flow)** — 2026-04-30 sim run confirmed the app launches past login + tab bar renders + automated tapping reaches customer detail via screenshot. The cliclick regression from 2026-04-27 has a working path again on iPhone 16 sim. *Effective state:* upper-flow validation is no longer blocked; only deep-flow estimate-edit calibration remains (now its own line above).
- **pitagents: Push backend commits to Railway (mostly)** — 2026-04-30 14:00 deploy confirmed PATCH /reports/{id}/estimate live in prod. *Effective state:* the pipeline works; whether the older `a504cf3` + `6307b71` rode along is the only outstanding unknown (now framed as a confirm-task above, not a block).
- **pitagents: Reconcile InspectScreen spec vs. implementation** — 2026-05-01 verified by execute run (read-only QA). Both platforms implement the 2-step flow. No blocker remained.
- **pitagents: `RecordingActivity.BASE_URL` Railway URL fix** — 2026-05-01 by execute run. Commit `8b02e2b` on `pitagents/main` (sandbox-local — local cron will sync to GitHub).

## Stalled (Soft) — Not Technically Blocked

*None as of 2026-05-01 evening. car-parts top-6 chains shipped 2026-04-25; per-location enrichment is a P2 with a clear next step (Google Places API investigation).*

## Watch — Potential Blockers for W18 / W19 P1

- **iOS sim deep-flow tap calibration is fragile** — Upper flow worked on 2026-04-30; deep navigation didn't. Pattern matches the 2026-04-26 → 2026-04-27 cliclick regression: pixel-driven taps + per-build coord recalibration is recurring fragility. Strongly suggests committing to `xcrun simctl io input` + accessibility identifiers instead.
- **Shipped ≠ smoke-tested (4th instance budget)** — 2026-04-30 shipped a substantial voice + reports + estimate stack to `pitagents.vercel.app` with deploy-time fixes (Vercel framework misconfig, lightningcss linux build) discovered only at deploy time. The voice chain bug surfaced after deploy too. Budget for more runtime-only bugs in the multi-step chain user-test and Android E2E.
- **AIBrain daily-cycle scheduler missing morning runs (3 days)** — No `chore(plan)` commits since 2026-04-28; daily reports for 04-29 / 04-30 / 05-01 weren't written. Sync + execute fired today (00:15 + 00:20) but plan didn't. Filed as a P3 backlog item; if the gap continues, escalates to P2.
  - **2026-05-02 update — escalated to P2 in `tasks/backlog.md`.** Today: morning-plan fired late at 12:09 UTC; **execute did not fire at all.** That makes 4 consecutive scheduled days with at least one cycle phase missing. Pattern is no longer noise. Next escalation: if 2026-05-03 also misses execute, promote to P1.
  - **2026-05-03 update — promoted to P1 in `tasks/backlog.md`.** W18 weekly rollup independently confirmed the pattern: execute slot has missed in 5 of the last 7 days, and Saturday's evening retro ran against stale (pre-sync) data and missed 35+ tasks of work. Now scoped as three sub-tasks: (a) investigate execute-slot silent skips via `mcp__scheduled-tasks__list_scheduled_tasks`, (b) **add a 19:50 UTC sync trigger** to feed the 20:00 retro (mirrors 7:50 UTC sync feeding 08:00 plan) — this is the architectural fix, the "late sync addendum" pattern in morning-plans is just a workaround, (c) gate cycle scripts behind `lsof | grep -q '.git/.*\.lock'` to prevent concurrent-session collisions.

## Friction (Self-inflicted — not owner-blocking)

- **Sandbox cloud runs cannot unlink files in `.git/`** — Every scheduled-task run leaves `.lock.stale.*` residue (the fuse mount allows file creation but blocks delete). Morning-plan workaround is `mv .git/index.lock .git/index.lock.stale.<ts>`. Tracked in P3 backlog. Recurring every day this week.
- **Sandbox cannot push to origin** — No GitHub credentials inside the scheduled sandbox; every `git push` from a triggered run fails with `fatal: could not read Username for 'https://github.com'`. Local cron has been catching up. Tracked as part of the same P3 cleanup.
- **Sandbox cannot push to other repos either** — `pitagents/main` commit `8b02e2b` from 2026-05-01 also sits sandbox-local awaiting local cron sync. Same root cause.
