# Task Backlog

> **2026-05-04 20:00 UTC update (Monday evening-retro run, W19 day 1):** Heavy pitagents day shipped overnight + early morning. **3 P1 items resolved by today's pitagents ships:** (a) Run SQL to update Technician agent system prompts → done via migrations 0029 + 0030; (b) Confirm Railway deployed migrations beyond 0023 → confirmed live via post-deploy verification on `/reports/{id}`; (c) iOS technician-chat voice transcription's 413 root cause → fixed via `_strip_base64_images()` shipped 05-03 evening. Marked complete + moved to Completed Recent. **2 new P1 items added:** (i) Verify migration chain on `feat/ios-technician-chat` merge — main now has 0023 → 0028 → 0029 → 0030 (shipped today), branch has 0023 → 0024 → 0026; sequencing must be reconciled before merge. (ii) Verify iOS Stop button works on physical device — code change builds, hardware verification pending. **Cycle-state update:** today's 8 AM morning-plan AND 9 AM execute both *missed* (7th silent execute miss in 9 days; only the 4th complete morning-plan miss). The 02:45 UTC sync ran early-off-cycle (likely Joe's local cron) so this evening retro had fresh data — but only because of the off-cycle pre-sync, not because the architectural fix landed. **Scheduler audit P1 promoted again:** sub-task (a) extended from "audit execute slot" to "audit execute + morning-plan slots both," and sub-task (b) "add 19:50 UTC sync trigger" promoted from sub-task to standalone P1 given today is the 4th iteration of the stale-evening-retro pattern + the highest-cost iteration to date (yesterday's retro missed 3 substantial 05-03 sessions including the entire chat-report unification ship). Tomorrow's single most important *sandbox-runnable* item: scheduler audit, both slots, immediately upon any sandbox session firing.
>
> **2026-05-03 20:00 UTC update (Sunday evening-retro run):** No movement on any P1 today. No new project journal entries from any project synced today; no memory deltas after the 11:50 UTC sync; no `chore(execute)` commit fired today (6th silent execute-slot skip in the last 8 days). End-of-day backlog state matches the morning-plan-run state below — backlog is current as written, nothing to add or move. The W18 weekly rollup written this morning (`reports/weekly/2026-W18.md`) confirms the strategic priority for W19 is to optimize the *review/approval* loop, not the implementation loop. **Carrying into 2026-05-04 Monday W19 day 1:** all 6 owner-gated P1s, the AIBrain scheduler audit P1 with three concrete sub-tasks, the new P2 "decompose 3 product pillars into 2–4 week roadmap," all 7 device/IDE-bound items, plus the buildco Phase 2 kickoff decision. The single most important *sandbox-runnable* item for tomorrow is the scheduler audit sub-task (a) — `mcp__scheduled-tasks__list_scheduled_tasks` to investigate why `aibrain-execute` keeps silently skipping its 9am fire window.
>
> **2026-05-03 ~12:00 UTC update (Sunday morning-plan run, with W18 weekly rollup):** This morning's 7:50 UTC sync brought down a much fuller picture of 2026-05-02 than yesterday's 8pm retro saw. Saturday actually shipped three substantial sub-projects: **feat/settings-panel** (10 tasks, **merged to main**, 21 commits), **feat/ios-technician-chat** (15 tasks, build succeeded, 15 commits ahead of main), and the **homepage/login/demo subtrack** (9 tasks, on the same `feat/ios-technician-chat` branch). New memory entries: `alembic_migration_chain.md`, `feedback_ios_sourcekit.md`, `project_homepage_login_demo.md`, `project_product_vision.md`. **Major backlog changes today:** (1) settings-panel marked complete (was P1), moved to Completed Recent. (2) Five new P1s surfaced from Saturday's work — merge `feat/ios-technician-chat`, Google Cloud Console OAuth setup, run technician-prompts SQL, physical-device iOS test, owner approval of 3-pillar mockup, follow-up on cut-off connected-ecosystem sentence. (3) Migrations 0024 + 0026 added to the Railway-confirmation P1 (will apply on the next deploy after merge). (4) **Scheduler audit promoted P2 → P1** — the W18 rollup independently confirmed the pattern is a regression, and the architectural fix (add 19:50 UTC sync trigger to feed 20:00 retro) is now a concrete sub-task. (5) New P3: CastWord watch (first journal entry yesterday, no code yet).
>
> **2026-05-02 20:00 UTC update (evening-retro run — KNOWN STALE, see late-sync addendum to 05-02 daily report):** No movement on any P1 today. No new development sessions logged in any project journal; no new memory entries since 11:50 UTC; no `chore(execute)` commit fired today (the 9am execute slot has now silently skipped on 4 consecutive scheduled days). End-of-day backlog change: **escalated "AIBrain: Audit daily-cycle scheduler" from P3 → P2** because the gap is now a 4-day pattern, not noise. *Note: this evening retro ran against pre-sync data and missed Saturday's 35+ shipped tasks; the morning-plan run on 05-03 has now corrected this view.*
>
> **2026-05-02 ~12:00 UTC update (morning-plan run):** Today's 11:50 UTC `aibrain-sync` brought in the rest of 2026-05-01's pitagents journal — a much larger body of work than the 20:00 evening retro saw. Late-evening 2026-05-01 shipped: feat/autoshop-web (36 tasks across 5 plans, merged `d293c5b`), Shop Agent Personas (7 tasks, `0cb3a34`), seed_demo.py (1,096 lines, `5c155f3` + UUID hex fix `1cea1e7`), POST /admin/seed trigger (`78c44e0`), Vercel `web/vercel.json` removal (`97f0045`). Plus the feat/settings-panel worktree was opened with Task 1 (DB-backed auth + /me + /profile + /password) implementer-done, code-quality review pending; Tasks 2–10 carry to today. Two new memory entries added: feedback_vercel_nextjs_subdir.md, feedback_react_infinite_loop_debugging.md. P1 reordered to surface the new deploy-confirmation and seed-trigger tasks — they're ahead of the older Railway commit-state question because they're more time-sensitive (13 fresh migrations, agent personas migration, demo data trigger).
>
> **Earlier 2026-05-01 ~12:00 UTC update (delayed morning-plan run):** Midday `aibrain-sync` (`4c7b529`) brought in journals showing Joe shipped two substantial pitagents web tracks during the morning — sleek redesign (9 tasks, merged via `da27f2e`) and dashboard / Home tab (5 tasks, merged via `abddfc2`). Neither was on the backlog as a P-anything item — they were greenfield work driven directly. Logged in `## Completed (Recent)` below.

## Priority 1 (Today — 2026-05-04 Monday, W19 day 1)

- [ ] **pitagents: Verify migration chain on `feat/ios-technician-chat` merge plan** *(NEW 2026-05-04)* — Main now has 0023 → 0028 → 0029 → 0030 (shipped today as direct-to-main pushes for chat-report unification + photo-handling system prompt). Branch `feat/ios-technician-chat` has 0023 → 0024 → 0026 still. Two parallel histories; merging will hit a sequencing conflict. **First sub-task (sandbox-runnable):** `cd ~/workspace/projects/pitagents && git fetch origin && git diff origin/main feat/ios-technician-chat -- alembic/versions/` to see divergent files; `alembic history` on each branch to see chain shape; produce a rebase plan (renumber 0024 → 0031, 0026 → 0032, update each file's `down_revision`). **Second sub-task (owner-driven):** apply rebase or alternative resolution; verify build + tests; merge.
- [ ] **pitagents: Verify iOS Stop button works on physical device** *(NEW 2026-05-04)* — Today's iOS Stop button code change (`AgentChatViewModel._sendingTask` + `cancelSending()`, `AgentChatView.inputBar` red `stop.circle.fill` swap, `TechnicianInputBar` Send → Stop capsule) builds successfully but cancellation behavior unverified on hardware. Owner runs technician chat on iPhone SE, sends a message, taps Stop mid-generation, confirms optimistic message disappears + input bar reverts. Repeat with image-attached send.
- [ ] **pitagents: Merge `feat/ios-technician-chat` into main** — Branch is 24+ commits ahead of main (15 iOS technician chat + 9 homepage/login/demo). Build succeeds. All 16 backend tests pass. TypeScript compiles cleanly. **Pre-merge gates:** (1) owner completes Task 9 (Google Cloud Console OAuth client ID setup); (2) migration-chain reconciliation per the new P1 above. Once merged + chain reconciled, the renumbered migrations auto-apply on next Railway deploy. *Owner-driven; sandbox unlikely to take this autonomously without final review pass + Google credentials.*
- [ ] **pitagents: Google Cloud Console OAuth client ID setup (Task 9 of homepage/login/demo)** — Owner needs to create an OAuth 2.0 Client ID and set `GOOGLE_CLIENT_ID` (Railway backend env) + `NEXT_PUBLIC_GOOGLE_CLIENT_ID` (Vercel + `.env.local`). Without this, `/login` Google button won't work and `POST /auth/google` skips the audience check (dev fallback). **Hard prerequisite** for the merge above. *Owner-only step (Google Cloud Console).*
- [ ] **pitagents: Trigger seed_demo.py against Railway prod and validate 7-customer narrative renders** — `POST /admin/seed` endpoint is live with `X-Admin-Secret: demo-seed-2026`. Once `1cea1e7` deploys: `curl -H "X-Admin-Secret: demo-seed-2026" -X POST https://backend-production-5320.up.railway.app/admin/seed`. Then validate the 7 demo customers (James Parker, Sarah Chen, Marcus Johnson, Emily Rodriguez, David Kim, Linda Thompson, Robert Martinez) appear in the Customers page, each with realistic vehicles + service history per the seed script narrative. *Owner-gated for the curl + UI verify.*
- [x] **pitagents: Run SQL to update existing Technician agent system prompts in DB** — *Completed 2026-05-04 via migrations 0029 + 0030 shipped to main + Railway.* Migration 0029 directly UPDATEs the Technician agent row to use the `report_builder` tools registry; migration 0030 UPDATEs the system prompt with the new PHOTO HANDLING section + numbered workflow steps 4-7. No manual SQL needed.
- [x] **pitagents: Confirm Railway has cleanly deployed `d293c5b` + `0cb3a34` + `1cea1e7` + 0024 + 0026 once branch merges** — *Effectively completed 2026-05-04 for migrations beyond 0023.* Today's three direct-to-main pushes (0028, 0029, 0030) all applied cleanly on Railway after the `00c883b5` SQL syntax fix; verification queries against `/reports/{id}` confirm Technician agent has the new tool list and system prompt. Migrations 0024 + 0026 (still on `feat/ios-technician-chat`) sit unmerged — they're now part of the merge-time migration-chain reconciliation P1 above, not a deploy-state question.
- [ ] **pitagents: Owner-validate the 3-pillar homepage mockup before writing the spec** — Visual mockup at `.superpowers/brainstorm/55140-1777773447/content/three-pillars-mockup.html` (or served at `http://localhost:63942/three-pillars-mockup.html`). 3-column equal-weight cards, each with a chat preview. Pillar 1: AI Technician (brake diagnosis). Pillar 2: Owner AI Crew (agent chips + revenue query). Pillar 3: Connected Vehicle History (consumer asks about car). **Decision needed:** approve the layout / iterate / take a different visual direction. *Owner-only.*
- [ ] **pitagents: Owner follow-up on the cut-off connected-ecosystem sentence (2026-05-02 21:30 vision input)** — Pillar 3 of the product vision was stated as "Vehicle History for consumers + Connected Ecosystem: any car's history is queryable — both shop and consumer side can pull it" with the next sentence cut off. Need the rest of that thought to scope Pillar 3 properly. *Owner-only.*
- [ ] **pitagents: Test iOS Technician Chat on physical device — camera, VIN scanner, voice transcription, video recording** — `feat/ios-technician-chat` build is on iPhone SE (A0BA0276-F03D-5300-95C7-21F1360A6EB5). Simulator can't exercise camera/microphone/video. Specifically validate: `CameraCaptureView` (multi-photo + thumbnail strip + Done/Cancel), `VINScannerView` (horizontal viewfinder + capture), `AVAudioRecorder` voice transcription (raw bytes + `Content-Type: audio/x-m4a` to backend, fixed Sat), `VideoRecorderView` + torch cancel fix. *Owner-gated (physical device).*
- [ ] **pitagents: Confirm Railway deploy state for `a504cf3` + `6307b71` and the 2026-04-30 feat cluster** — Older question: did `a504cf3` (end-to-end report generation pipeline, 2026-04-26) and `6307b71` (Twilio resilience + InspectScreen, 2026-04-27) ride along on the 2026-04-30 14:00 deploy, or are they still unshipped? Largely subsumed by the 2026-05-02 Railway-state confirmation above (any prod logs query will show this too), but called out separately because if those older commits *aren't* on Railway, prod still returns HTTP 500 on `POST /vehicles/{id}/messages` when Twilio is flaky. *Owner-gated (no Railway creds in sandbox).*
- [ ] **pitagents: User-validate multi-step voice chain + clean up debug logs** — 2026-04-30 final session shipped 3 commits with debug `console.log` statements deliberately left in `voiceControlController.ts` and related voice files. Joe was asked to test PTT with "go to reports and find the Honda Civic" and paste the `[voice] chain-check` console output. On success: remove all debug `console.log` lines; on failure: paste output for diagnosis. *Owner-gated test, sandbox-cleanable cleanup.*
- [ ] **pitagents: iOS Simulator deep-navigation calibration for report estimate flow** — 2026-04-30 sim run confirmed login + customer detail navigation working again on iPhone 16 sim (the cliclick regression from 2026-04-27 has a working path). Outstanding: deep-flow taps to the report estimate-edit screen "needed coordinate calibration for secondary display." Two paths: (a) recalibrate, (b) switch driver to `xcrun simctl io <UDID> input` + accessibility identifiers (more reproducible). *Owner-gated (Xcode); sandbox can't run iOS Simulator.*
- [ ] **AIBrain: Scheduler audit (now covering BOTH execute + morning-plan slots)** *(promoted again 2026-05-04)* — Today the morning-plan also missed (4th total miss in 8 days), and execute hit its 7th silent miss in 9 days. Sub-task (a) extends from "audit execute slot" to "**audit execute + morning-plan slots both**": `mcp__scheduled-tasks__list_scheduled_tasks` to confirm both are enabled + check last error/run for each. Sub-task (c) **Gate cycle scripts behind `lsof | grep -q '.git/.*\.lock'`** to prevent concurrent-session collisions remains.
- [ ] **AIBrain: Add 19:50 UTC sync trigger to feed 20:00 evening retro** *(promoted 2026-05-04 from sub-task → standalone P1)* — Yesterday's retro missed 3 substantial 05-03 sessions (18:45 / 20:30 / 23:00) including a complete chat-report unification ship (10 tasks, 9 commits). 4th iteration of the stale-evening-retro pattern at the highest cost yet. The "late sync addendum" pattern in morning-plans is just a workaround; this trigger is the architectural fix.
- [ ] **pitagents: Monitor real inspection results for per-finding photo matching quality** — Joe's 2026-04-27 05:00 session shipped the photo-to-finding matching fix (chain-of-thought prompt + stable-S3-URL identifiers in `findings.photo_url`). Watch live PDFs over the next few inspections to confirm photos land on the correct finding cards instead of the unassigned gallery. *Live-data-gated.*
- [ ] **pitagents: Sub-project C — voice editing of report line items** — modify price/quantity via voice. Spec not yet written. Now that A (voice nav) + B (reports rewrite) are live in prod and the multi-step chain bug is fixed, C is the natural next slice. **First step:** brainstorm + spec, not implementation. **Note:** Saturday's product-vision input may reframe this — Sub-project C is implementation-level, but the new "AI Crew" pillar suggests the next decomposition should be vision-level (which crew member handles each surface), not feature-level. Worth a re-scope conversation.
- [ ] **buildco Phase 2 kickoff** — Task management API (create/assign from CEO Agent chat), daily briefing push notifications (iOS), brand kit generation via image API. Mobile-smoke prerequisite is now meaningfully satisfied (Android Pixel 10a 2026-04-27, iOS sim 2026-04-30 confirmed login + nav + estimate edit on web; iOS technician chat builds + launches on iPhone SE 2026-05-02). **Decision Joe needs to make:** kick off in parallel with the now-bigger pitagents pipeline, or finish current owner-gated steps first.
- [x] **pitagents: feat/settings-panel — Tasks 1–10 + merge to main** — *Completed 2026-05-02 by owner-driven SDD shop.* All 10 tasks shipped, 21 commits merged to main. Slide-in settings panel with 7 sections (Account, Shop Profile, Appearance, Booking, Notifications, Integrations, Agents & AI). New backend endpoints: GET /auth/me, PATCH /auth/profile, PATCH /auth/password, GET/PATCH /appointments/my-config, GET/PATCH /settings/profile. ShopSettingsResponse expanded with `has_*` boolean flags. Pre-merge final review caught: stripe_secret_key field-name mismatch + AccountSection setTimeout leak.
- [x] **pitagents: Apply iOS `MessageResponse` optional fix (code-review #4)** — *Completed 2026-04-28 09:03 by execute run.* Commit `01f6fde` on `pitagents/main` — added `externalId: String?` + `sentAt: String?` and `CodingKeys` entries `external_id` / `sent_at` in `ios/AutoShop/Network/APIModels.swift:222`. Forward-compat for future UI (delivered-at, external_id dedup) and parity with Android.

## Priority 2 (This Week — W18)

- [ ] **pitagents: Vercel GitHub auto-deploy integration is broken — investigate / repair** — `cd web && vercel --prod --force` from local is the workaround Joe has been using since at least 2026-05-01. Possible causes: orphaned `vercel.json` in repo (already removed), framework misconfig that re-emerged (was `null` in 2026-04-30, fixed to `nextjs`), GitHub app reauth needed, or the repo's Vercel project no longer wired to the GitHub webhook. **First step:** check the Vercel project's "Git" settings page in the dashboard.
- [ ] **pitagents: `Campaign.shop_id` is a String, not a UUID FK — inconsistent with other models** — Decision flagged in 2026-05-01 P5-T2 implementation: Campaign uses `shop_id: str` (no FK) for "simpler multi-tenant queries," while every other shop-scoped model uses `shop_id: UUID` with `ForeignKey("shops.id")`. The String approach blocks cascade deletes + ORM-level join optimization. Decide: convert Campaign to UUID FK (low risk, single migration), or accept the inconsistency long-term and document the rationale. *Should be resolved before campaigns get heavy production usage.*
- [ ] **pitagents: Investigate Twilio HTTP 500 root cause on Railway** — Backend now isolates the failure (commit `6307b71` catches Twilio/SendGrid exceptions, saves message with `external_id=null`), but the underlying integration is still broken in prod. Likely causes: (a) credential / env-var drift between local and Railway, (b) Twilio account status, (c) WhatsApp sender / template approval gone stale, (d) rate-limit hit. Check Railway logs + Twilio dashboard. Source: `sync/journals/projects_pitagents/2026-04-27.md` Session 09:30.
- [x] **pitagents: Reconcile InspectScreen spec vs. implementation** — *Verified 2026-05-01 by execute run.* iOS `InspectTabView` + `CustomerVehiclesInspectView` implements the 2-step `customer grid → vehicle list → RecordingView` flow per `recording-inspect.feature`. Android `InspectScreen` in `AppNavigation.kt:244` implements the same 2-step flow with `TopAppBar` back nav per commit `6307b71`. Minor non-blocking divergence: iOS uses a `LazyVGrid` (matches Gherkin "grid"); Android uses `LazyColumn` (a list, not a grid). Both pass the navigation behavior the spec actually exercises; reading the Gherkin literally, the customer-card layout differs.
- [x] **pitagents: Fix `RecordingActivity.BASE_URL` hardcoded to `http://10.0.2.2:8000`** — *Completed 2026-05-01 by execute run.* Pointed `RecordingActivity.BASE_URL` to `https://backend-production-5320.up.railway.app` so the recording flow works on physical devices (Pixel 10a smoke). Mirrors the URL `ApiClient.kt` and `ReportDetailScreen.kt` already use. Commit `8b02e2b` on `pitagents/main` (sandbox-local; awaits Joe's local cron sync to GitHub). A proper unification through BuildConfig across all 3 hardcoded sites is a follow-up cleanup task.
- [ ] **pitagents test infra cleanup** — (1) Create `test_autoshop` DB with credentials matching `postgresql+asyncpg://test:test@localhost/test_autoshop` to fix 14 asyncpg test errors. (2) Remove or update stale `test_full_inspection_flow` E2E mock (`run_inspection_agent` no longer exists in `src/api/sessions.py`).
- [ ] **pitagents: Add Qdrant parts data so Qdrant fallback estimate has better pricing** — Today's session noted Qdrant returns $0 for parts not in the catalog, which collapsed estimate totals from ~$935 → $240. `finalize_quote` currently overrides with the quote's Claude-generated estimate, but improving the Qdrant fallback would make the report total robust without that override. Source: 2026-04-27 morning journal "Next steps".
- [ ] **pitagents: Unify `_generate_line_items` and `generate_estimate` Claude calls** — Two separate Claude calls today; consolidating into one would cut latency and cost. Source: 2026-04-27 morning journal "Next steps". *Soft-priority; only attempt after mobile smoke passes.*
- [ ] **car-parts: Per-location phone enrichment** — Investigate Google Places API (or similar) for the ~2,000 recyclers with empty phone fields. Corporate-level phones already added to top-6 chains.
- [ ] **stlab: Continue post-CMDB work** — phase 1 (CMDB ingestion + data model + groups + gap reporting) shipped W17. Next phase TBD; check stlab journals for direction when work resumes there.
- [ ] **Configure Cowork with useful skills and connectors** — Explore available plugins, MCP connectors (Slack, Linear, GitHub, etc.), and skills to set up a productive working environment. *Carried since W14.*
- [ ] **Understand the difference between Skills, Connectors, and MCP** — Learn how these three concepts relate in the Claude ecosystem. See reference notes below. *Carried since W14.*
- [ ] **Decompose the three product pillars (AI Technician / Owner AI Crew / Consumer Vehicle History + Connected Ecosystem) into a 2–4 week roadmap** *(new 2026-05-03 from W18 rollup)* — Saturday's owner vision input reframes the next phase. Current backlog is implementation-level (Sub-project C voice editing, settings-panel iterations); the new vision suggests a higher-level decomposition is needed. **First step:** brainstorm session translating each pillar into 3–5 concrete near-term initiatives, then sequence into a Now/Next/Later roadmap. Pillar 3 needs the cut-off connected-ecosystem sentence first.

## Priority 3 (Later)

- [ ] **CastWord (NEW PROJECT) — watch for direction** — `sync/journals/projects_CastWord/2026-05-02.md` is the first journal entry. Single 00:00 session: owner asked how to run the app; pointed to README.md. Project has a single initial commit, README.md + CLAUDE.md present. No active task. Filed for awareness; promote when Joe drives it forward. Source: `sync/journals/projects_CastWord/2026-05-02.md`.

- [ ] **openclaw — new project, watch for direction** — `sync/journals/projects_openclaw/2026-04-25.md` shows a single 20:24 Friday session: empty project, user asking how to install OpenClaw. No code yet, no follow-up sessions through Sunday. Filed for awareness; promote when Joe drives it forward. Source: `sync/journals/projects_openclaw/2026-04-25.md`.
- [ ] **Fix sandbox `.git` lock accumulation** — Cloud runs cannot `unlink` files inside `.git`, so every run leaves `.lock.stale.*` residue (and on Apr 24 morning the initial `git checkout` was blocked by a stale `index.lock` from the 2af7213 sync). 2026-04-26 evening retro hit the same issue (`unable to unlink .git/index.lock: Operation not permitted`). Either gate git write-ops behind a lock cleaner, or run sync/retro only from the local cron where unlink is permitted. Source: interview_AIBrain journal 2026-04-23 "Friction notes".
- [ ] **Confirm sync pipeline health** — Overnight sync on 2026-04-23 correctly caught the pitagents 23:09 session that was missed by the prior evening retro. *Re-validated 2026-04-27 morning:* the 7:50am sync correctly brought down a large block of pitagents Sunday journal data that last night's evening retro had missed (7 substantial Sunday sessions). The morning-plan re-run mechanism worked — incomplete evening retros are recoverable as long as the morning sync catches up. Continue monitoring.

## Completed (Recent)

- [x] **pitagents: Chat Report Unification — all 10 tasks live on Railway end-to-end** *(2026-05-04 / continuing from 05-03 23:00 session)*. Backend: migration 0028 (Report.session_id nullable), `report_tools.py` (`create_report`, `add_report_item`, `list_report_items`), `report_builder` registered in `tool_registry.py`, `reports.py` null guards, `assistant_blocks.py` REPORT_BUILD replaces QUOTE_BUILD. iOS: `ChatHistoryItem` extension parses `[REPORT:uuid]` → `reportId`; `AssistantView` uses `msg.reportId`; `ReportCardBubble` rewritten to load `ReportDetail` directly + open `ReportDetailView`. Migration 0029 SQL syntax fix (`tools = :tools::jsonb` → `CAST(:tools AS jsonb)`) cleared 3 consecutive Railway deploy failures via `00c883b5`. Full Technician chat → `report_builder` → `[REPORT:uuid]` card → `ReportDetailView` flow operational in prod.
- [x] **pitagents: Photo-to-finding pipeline — 4 root causes shipped + verified live on Railway** *(2026-05-04)*. (1) System prompt gap → migration 0030 (new Technician prompt with PHOTO HANDLING section + workflow steps 4-7). (2) `chat.py` raw private S3 URLs → 400 from Anthropic → fixed with `_presign_s3_urls()` helper generating 1-hour presigned URLs before Anthropic call. (3) `reports.py::_to_staff_detail` raw S3 URLs → iOS AsyncImage 403 → made async + generates 7-day presigned URLs at serve time. (4) S3 SigV2 → 403 for post-2020 buckets → fixed with `Config(signature_version="s3v4")` + 7-day expiry cap (SigV4 max 604,800s). End-to-end verification on live Railway: photo upload ✅ → `add_finding(photo_url=...)` ✅ → DB populated ✅ → `GET /reports/{id}` returns 7-day SigV4 presigned URL ✅ → URL accessible HTTP 200 ✅.
- [x] **pitagents: iOS Stop button for LLM generation** *(2026-05-04)*. `AgentChatViewModel._sendingTask: Task<Void, Never>?` + `cancelSending()`; `send()` and `sendWithImages()` non-async; CancellationError silently removes optimistic message. `AgentChatView.inputBar` swaps red `stop.circle.fill` for send button while `vm.isSending`. `TechnicianInputBar` swaps compact send + expanded "Send" capsule for "Stop" capsule. iOS BUILD SUCCEEDED. Hardware verification of cancellation behavior pending (now its own P1 above).
- [x] **pitagents: 413 "request too large" fix on chat** *(2026-05-03 18:45 — synced 2026-05-04)*. Added `_strip_base64_images()` to `backend/src/api/chat.py`: base64 image data is stripped before persisting to ChatMessage DB, replaced with `[image]` text placeholder. Prevents history payload from growing unboundedly on subsequent turns. Deployed to Railway via push to main.
- [x] **pitagents: Demo script for two-role flow** *(2026-05-03 20:30 — synced 2026-05-04)*. `docs/demo-script.md`: technician on iPhone → service advisor on web, BMW power steering fluid leak scenario with David Chen as customer. Covers VIN scan, voice description, photo capture, AI report generation, price editing, send to advisor, voice navigation on web, report retrieval, customer share. Includes a pre-demo checklist.

- [x] **pitagents: feat/autoshop-web — 36 tasks across 5 plans, merged to main (`d293c5b` fast-forward)** — 2026-05-01 late-evening + overnight. Full AutoShop web platform vertical: Plan 1 Job Cards & Invoices (11 tasks, MAX-based numbering with `UniqueConstraint("shop_id", "number")` DB safety net), Plan 2 Appointments + Service Reminders (6 tasks), Plan 3 Payments / Inventory / Vendors (6 tasks), Plan 4 Time Tracking + Accounting (7 tasks), Plan 5 Diagnose + Marketing (6 tasks; CarMD `/maintainance` typo preserved). 87 files added/modified: 12 backend API modules, 12 migrations 0010–0021, 15 models, 13 test files, 13 frontend pages, 25 components. TypeScript clean, Next.js build clean, 7/7 tests pass. Backend + frontend ready to deploy (Railway + Vercel). Bugs caught + fixed during review: COUNT→MAX numbering, .isoformat() serialization, boolean nullability, get-or-create race condition, typed list schemas, Literal status validation, UUID cast in audience count, exclude_unset vs exclude_none, Promise.allSettled for partial diagnose results, Parts Needed from diagnosis.part not repairPlan.
- [x] **pitagents: Shop Agent Personas — 7 tasks shipped, pushed (`0cb3a34`)** — 2026-05-01 22:00. DB-driven agents replacing hardcoded `AGENT_GRAPHS`. `ShopAgent` model + migration 0022, static `TOOL_REGISTRY` (vin_lookup, quote_builder, parts_search, shop_data) + `build_tool_schemas_and_executor`, `/agents` CRUD + `/agents/tools` registry, lazy seed of 5 defaults (Service Advisor, Technician, Parts Manager, Bookkeeper, Manager) on first request per shop, `chat.py` rewrite with `_get_agent_graph` DB-lookup-and-cache, AgentList.tsx TanStack Query rewrite with capability tooltip, Settings page Agents tab with full CRUD form. Railway runs migration 0022 on next deploy.
- [x] **pitagents: seed_demo.py — 1,096 lines, deterministic, all 24 tables (`5c155f3` + `1cea1e7` UUID hex fix)** — 2026-05-01 09:00. 7-customer narrative covering shops/users/shop_settings/booking_configs/customers/vehicles/vendors/inventory_items/purchase_orders/job_card_columns/job_cards/invoices/invoice_payment_events/inspection_sessions/media_files/reports/quotes/appointments/service_reminder_configs/service_reminders/time_entries/expenses/campaigns/shop_agents/chat_messages/customer_messages. Single transaction; FK-aware nuke order; asyncpg direct (no ORM); `postgresql+asyncpg://` → `postgresql://` auto-conversion. UUID hex fix replaced 13 mnemonic prefixes (v/j/i/p/s/r/q/t/x/m/o → b7/b8/b9/c4/c5/c6/c7/c8/c9/d4/d5/d6/d7/d8). `POST /admin/seed` trigger live with `X-Admin-Secret: demo-seed-2026` (`78c44e0`).
- [x] **pitagents: Vercel `web/vercel.json` removed (`97f0045`)** — 2026-05-01. The harmless-looking `ignoreCommand`-only file broke Next.js `app/` directory detection because Vercel root was already configured to `web/`. Fix: remove the file; configure ignoreCommand via dashboard.
- [x] **pitagents web: Sleek redesign merged to main (`da27f2e`)** — 2026-05-01 morning. 9-task subagent-driven implementation. CSS tokens (`--accent-glow`, `--bg-overlay`, `.glass-panel`, `.glass-card`); `useTheme` hook with localStorage + live `--bg-overlay` CSS-var update; `pravatarUrl` avatar utility; `AppBackground` (fixed photo + overlay); `SettingsDropdown` (theme picker + sign out, gear-cog SVG); full `AppShell` rewrite (frosted nav, SVG icons, Pravatar avatars); Login redesign (frosted glass on photo bg) with double-submit fix via `navigated` flag in finally; Customers / Reports / Inspect / Chat AgentList all on glass panels with amber accent; cleaned up dead `colorFor`/`initials` helpers and indigo color leak in inspect. All 11 routes compile cleanly.
- [x] **pitagents web: Dashboard / Home tab merged to main (`abddfc2`, fast-forward)** — 2026-05-01 morning. 5-task subagent-driven implementation. `tiles.tsx` (6 group configs, 23 tiles — 7 live / 16 coming-soon, 23 inline SVG icon components); `FeatureTile` (live = Link + hover, soon = inert div + badge); `GroupSection` (coloured-dot header + flex tile row); `DashboardPage` mounted at root `/`; `AppShell` Home tab prepended with corrected active detection (`exact: true` for `/`, `pathname.startsWith()` for others). Key boundary decision: dashboard components need `'use client'` because `GROUPS.icon` stores React component functions (non-serializable across the Next.js server/client boundary), but `tiles.tsx` itself stays server-renderable as data.
- [x] **pitagents web: Voice control layer (Sub-project A) + Reports page rewrite (Sub-project B) shipped to production at `pitagents.vercel.app`** — 2026-04-30. WebRTC voice via OpenAI Realtime API, 5 tools (`navigate_to_tab`, `select_agent`, `send_message`, `select_customer`, `select_report`), `VoiceControlWidget` with PTT + status ring; Reports page now uses glass cards + severity icons + voice_select param. `OPENAI_API_KEY` ephemeral-token exchange via `/api/session`. Two production-deploy fixes shipped alongside (Vercel project framework misconfig + lightningcss-darwin-arm64 removal).
- [x] **pitagents web: PTT voice navigation `mousedown→mouseup→click` race fix** — 2026-04-30 16:00. `mouseDownAt` ref guards the click handler; holds > 200ms suppress disconnect, short taps still toggle off.
- [x] **pitagents web: OpenAI Realtime multi-step tool chain bug fix (3 commits)** — 2026-04-30 final session. Session-scoped `toolExecutedDuringResponse` flag (resilient to null `responseId` from `output_item.done`); `tool_choice: 'required'` → `'auto'`; INSTRUCTIONS updated to batch all tools in one response. Debug `console.log` lines deliberately left in for user verification.
- [x] **pitagents backend: PATCH /reports/{id}/estimate live on Railway** — 2026-04-30 14:00. Editable estimate table on the Reports page; 5 tested cases pass.
- [x] **pitagents: Reconcile InspectScreen spec vs. implementation** — 2026-05-01 by execute run. iOS + Android both implement the 2-step `customer → vehicle → recording` flow per `recording-inspect.feature`. Minor non-blocking divergence on customer-card layout (iOS LazyVGrid vs. Android LazyColumn).
- [x] **pitagents: Fix `RecordingActivity.BASE_URL` hardcoded to `http://10.0.2.2:8000`** — 2026-05-01 by execute run. Pointed BASE_URL to `https://backend-production-5320.up.railway.app`. Commit `8b02e2b` on `pitagents/main` (sandbox-local; awaits local cron sync). BuildConfig unification across all 3 hardcoded sites is a follow-up cleanup task (P3).
- [x] **pitagents: Apply iOS `MessageResponse` optional fix (code-review #4)** — 2026-04-28 by execute run. Commit `01f6fde`.
- [x] **pitagents: Android `MessagesViewModel` + optimistic send + 7 Gherkin test plans + Pixel 10a smoke run** — Joe's 09:30 Monday session. Implemented `MessagesViewModel` with `StateFlow` and `ViewModelProvider.Factory`; placeholder-then-replace optimistic send with rollback on failure; fixed `!!` force-unwrap crash and `_isSending` race; refactored `MessagesTab` in `VehicleDetailScreen.kt` to use the ViewModel + `keyboardActions = KeyboardActions(onSend = { doSend() })` + `focusManager.clearFocus()` to fix keyboard dismiss. Wrote 4 unit tests covering optimistic insert, success replacement, failure rollback, blank-body guard. Wrote 7 Gherkin feature files in `pitagents/docs/test-plans/` covering all app flows. Built + installed on Pixel 10a; ran the test plan and got most scenarios PASS. Source: `sync/journals/projects_pitagents/2026-04-27.md` Session 09:30.
- [x] **pitagents: Backend Twilio HTTP-500 isolation + InspectScreen 2-step flow** — Joe's 10:10 Monday session, commit `6307b71`. (1) `customer_messages.py` now wraps the Twilio/SendGrid call in a `try/except`, logs a warning, and persists the message to DB with `external_id=null` — the user-facing "send message" no longer fails when the integration is down. (2) `InspectScreen` (Android) replaced single "Start Inspection" button with a full 2-step `customer list → vehicle list → RecordingActivity` flow; passes `VEHICLE_ID` and `CUSTOMER_NAME` extras alongside `SHOP_ID`; added `TopAppBar` with back navigation between steps. Source: `sync/journals/projects_pitagents/2026-04-27.md` Session 10:10.
- [x] **pitagents: 3 P0 mobile fixes** — Shipped 2026-04-27 9am execute run as 3 separate commits on `pitagents/main`. (1) `87a3fb7` `fix(ios): handle URL-safe base64 in JWT decoder` — replace `-`→`+` and `_`→`/` before `Data(base64Encoded:)` in `AppState.swift:decodeToken`. Without this the user saw blank email/role/shop_id on the Profile tab whenever the JWT payload happened to contain URL-safe-only chars. (2) `7b3c120` `fix(ios): make ChatHistoryItem.createdAt optional` — `String` → `String?` in `APIModels.swift:255` to match `backend/src/api/chat.py:115` returning `null` when `created_at` is missing; convenience init updated to `nil`; grep confirmed no callers read it. (3) `3ce3daf` `fix(android): show 'Session expired' on login screen after mid-session 401` — added `TokenStore.expireSession()` + `sessionExpired: StateFlow<Boolean>`; `ApiClient.kt` interceptor only calls `expireSession()` when a token was attached so login-time 401 still says "Invalid email or password"; `LoginScreen.kt` consumes the flag once via `LaunchedEffect` and clears it on any new login attempt. Completed 2026-04-27 by 9am execute run.
- [x] **pitagents: Inspection report PDF — per-finding photo attachment + chain-of-thought matching + unified `build_report` service** — Joe's 05:00 laptop session. Fixed 6 bugs: (1) iOS uses `PUT /quotes/{id}/finalize` not `POST /sessions/{id}/generate-report` (all `sessions.py` fixes were in the wrong code path), (2) presigned URLs stored in `findings.photo_url` caused all comparisons to fail → photos fell into unassigned gallery, (3) Claude strips query string from presigned URLs → added normalization step mapping bare URL to stable S3 URL, (4) `import anthropic` accidentally removed → 0 line items per quote, (5) iOS `POST /quotes` was sending transcript to quote but not session → empty session transcript at report time, (6) Qdrant returns $0 for unknown parts → `finalize_quote` now overrides with quote's Claude-generated estimate. Consolidated quotes + sessions report code paths into one `build_report` service. Source: `sync/journals/projects_pitagents/2026-04-27.md`. Completed 2026-04-27 morning.
- [x] **pitagents: Mobile code review pass** — Static review delivered at `pitagents/docs/mobile-code-review-2026-04-26.md`. Found 3 P0 runtime bugs (iOS URL-safe base64 JWT decoder, iOS `ChatHistoryItem.createdAt` non-optional vs. backend null, Android login error wording on token-expiry 401) + 5 P1 silent issues. Suggested fix order: P0s same-day before simulator run. Commit `78df578`. Completed 2026-04-26 by execute run.
- [x] **pitagents: API contract docs (enum values + endpoint catalog)** — Delivered at `pitagents/docs/api-contract.md`. Documents direction (`"out"/"in"`) + channel (`"wa"/"email"`) + role (`"owner"/"technician"`) + quote status + session status enums with line refs. Full endpoint catalog grouped by router. Wire-format conventions section. Open questions list. Commit `78df578`. Completed 2026-04-26 by execute run.
- [x] **AIBrain: W17 weekly rollup** — `reports/weekly/2026-W17.md` written via independent re-evaluation of all 7 daily reports + journals + memories. Three weekly learnings: SDD now proven across 3 sub-projects, cross-stack consumer = cheapest drift detector, an 11-day stalled P1 is a demote signal. Commit `3a2a869`. Completed 2026-04-26.
- [x] **pitagents: Sub-project 3 — Mobile Chat Interface (iOS + Android)** — Both apps fully implemented + committed in one session via subagent-driven development. **Android (12 tasks, 11 commits):** Kotlin/Compose scaffold, EncryptedSharedPreferences token store, OkHttp ApiClient with auth interceptor + 401 handling, sealed Screen routes + bottom nav, screens for Login/CustomerList/VehicleList/VehicleDetail (Messages+Reports tabs)/Assistant/Profile. **iOS (10 tasks, 10 commits):** XcodeGen-based project (iOS 17.0), @StateObject AppState gate, Codable APIModels, @MainActor APIClient reusing SessionAPI.baseURL, Security framework KeychainStore, JWT-decoding AppState, MainTabView with 3 tabs. Backend enum drift caught during integration: direction `"out"/"in"` (not `"outbound"`), channel `"wa"/"email"` (not `"sms"`). Code review = quality gate until simulator/emulator runs. Completed 2026-04-25.
- [x] **pitagents: Validate enterprise AI stack end-to-end + merge to main** — Validated 2026-04-25 9am run. Stack running (5 containers healthy). Few-shots seeded (12 examples, used OpenRouter for embeddings via `OPENAI_BASE_URL=https://openrouter.ai/api/v1` since OpenAI key quota exhausted). Chat SSE confirmed end-to-end. 100 tests pass. Merge was already on main (`32ececc`). Completed 2026-04-25.
- [x] **car-parts: Contact enrichment (top-6 chains validation chunk)** — Enriched 176 entries across 6 chains: LKQ (117, phone+email), Fenix (25, email), Aesop (15, website+phone), iPull-uPull (4, website), B&R (14, phone), Pick-n-Pull (1, phone+email). Committed `093f561`. Next: per-location phone enrichment via API. Completed 2026-04-25.
- [x] **pitagents: Enterprise AI architecture rebuild — 13 tasks via Subagent-Driven Development** — LangGraph 5-node ReAct graph (classify_intent → assemble_prompt → call_llm → validate_response → done), Qdrant vector store, dynamic prompt assembly, response validator, feedback critic, thumbs up/down UI, guardrails, semantic_parts_search, ingestion scripts, feedback API. 15 commits on `feat/enterprise-ai-architecture`. 45 tests passing. Code review caught + fixed: empty `intent` field, missing 403 test, QDRANT_API_KEY → SecretStr. Validation + merge moved to 2026-04-25 P1. Completed (implementation) 2026-04-24.
- [x] **pitagents: Parts catalog research — 30+ OEMs documented** — `docs/parts-catalog-research.md` covers American (Ford, GM, Stellantis), European (BMW, VW, Mercedes, Renault, Volvo, JLR, etc.), Asian (Toyota, Honda, Nissan, Hyundai/Kia, Mazda, etc.), Chinese (BYD, SAIC/MG, Geely, NIO, etc.). Resolves BMW ETK question: BMW AOS portal is FREE for non-dealers; autodb.cc offers €800 SQL dump for raw data. Mercedes EPC ($110/yr), VW Partslink24 (~$480/yr), Volvo VIDA ($210.80/yr), Toyota Microcat (free for indies) noted. US Big-3 = dealer-only. Completed 2026-04-24.
- [x] **pitagents: Quote Agent end-to-end smoke test + 4 runtime fixes** — Joe smoke-tested the Quote Agent in the evening and found 4 runtime bugs that the unit-test suite had missed: argument-order mistake in `create_quote` (`3f143cc`), QuoteSummary using wrong qty field + missing refetch after finalize (`be29322`), agent stream error handler silently swallowing exceptions (`191fbd0`), and `quoteId` not restored from chat history on reload (`c11c535`). Quote Agent now functional end-to-end. Completed 2026-04-24 evening.
- [x] **pitagents: Execute Sub-project 2 (Quote Agent)** — All 10 tasks shipped: Quote DB model + migration, 6 quote tools, quote agent, system prompt, API routes, QuotePanel + QuoteSummary frontend, tests. Refactored: consolidated into assistant with capability cards (`refactor(assistant)` commit). Deepgram transcribe bug also fixed. Source: pitagents journal 2026-04-23 + commits `dfd6c8e`, `8e77469`. Completed 2026-04-24.
- [x] **stlab: Continue relationship gap reporting + groups tab + port fix** — Both features were already fully implemented in `ui/pages/2_CI_Browser.py`: Groups tab (tab5) shows all groups with user counts, click to expand member list; Gap reporting in "Action Required" tab (tab4) shows "Unresolved References" table. Port 8000 conflict resolved: stlab moved to host port 8001 (`fix(port)` commit `7bc8293`). 80/80 unit tests pass. Completed 2026-04-24.
- [x] **pitagents: Fix transcribe endpoint 502 (Deepgram)** — Fixed: use correct Deepgram SDK method + error logging. Commits `4b7ab90` + `7cfe5c9` + `d4a806f`. Completed 2026-04-24.
- [x] **stlab: CMDB ingestion validation on freshly nuked data** — 80/80 unit tests pass (fixed Dockerfile to install dev deps); ingestion pipeline validated: 9 users, 3 devices, 6 apps; 3 device-user assignments confirmed. Completed 2026-04-23.
- [x] **pitagents: smoke test of Sub-project 1 + plan Sub-project 2 (Quote Agent)** — API smoke test passed (found+fixed 3 bugs: UUID auth IDs, model_dump extra fields, SSE save placement). Sub-project 2 plan written at `docs/superpowers/plans/2026-04-23-quote-agent.md` (10 tasks, 4–6h). Completed 2026-04-23.
- [x] **pitagents: Sub-project 1 — Web Chat UI** — All 15 tasks implemented: DB model, transcribe endpoint, upload endpoint, agent tools (VIN + shop DB), base/assistant/tom agents, chat API (SSE streaming), frontend API client, MessageBubble, VoiceButton, ImageAttach, ChatPanel, AppShell + AgentList, /chat route. 76/76 backend tests passing. Branch: `feat/web-chat-ui`. Completed 2026-04-23. Source: pitagents journal 2026-04-23.
- [x] **pitagents: initial stack bring-up + auth flow** — Full Docker Compose stack (db, redis, backend, web) up; 5 startup bugs fixed (pydantic_settings, bcrypt/passlib, pydantic[email], `/` → `/dashboard`, CORSMiddleware); `/login` + JWT + dashboard auth guard wired; creds `owner@shop.com` / `testpass` work. Completed 2026-04-22 night. Source: pitagents journal 2026-04-22.
- [x] **pitagents: repository README + quick-start guide** — Root-level README.md for new-developer onboarding. Completed 2026-04-22 AM. Source: pitagents journal 2026-04-22.
- [x] **stlab: CMDB ingestion + data model (phase 1)** — Pipeline fixes (code fence stripping, NullPool/Celery, recursive parser, `on_conflict_do_nothing` upsert dedup, model alias), 3 migrations (0003/0004/0006), docs/plans, test suite repairs (80/80 passing). Committed 4 commits to stlab/main via AIBrain execute `11a09cb`. Completed 2026-04-22.
- [x] **PitCrew: YouTube outreach expanded to 2,000 channels** — 76 BFS rounds, Cat 1–3 prioritized (Parts Resellers, Salvage/Part-Out, Car Flippers). Committed `a55fb12`. Completed 2026-04-12.
- [x] **PitCrew: Vercel build fix** — Clerk v7 prop rename + Next.js 16 `proxy.ts`. Committed `8ae0113`. Completed 2026-04-12.
- [x] **car-parts: Recycler directory page** — 3,623 recyclers loaded into `apps/web/src/data/recyclers.ts`, `/recyclers` page live, nav link added. Completed 2026-04-11.
- [x] **Interview rubric: Option B promotional platform question** — Full promo platform take-home problem added (POST /promotions, redeem, GET status, background job), with concurrency-specific rubric additions (concurrent redemption handling, validation ordering, error specificity, idempotency). Completed 2026-04-06/07.
- [x] **buildco: full-stack AI business platform** — scaffold, auth, admin, 18 API routes, live Claude API, iOS SwiftUI app with full test suite, completed 2026-04-06/07.
- [x] **RAG deep dive: PartFinder implementation** — Architecture reference + full 4-use-case spec (hybrid retrieval, model tiering, pgvector HNSW), completed 2026-04-07.
- [x] **Car-parts platform design** — gap analysis, 5-phase implementation roadmap, completed 2026-04-05
- [x] **Set up Apple Developer account** — resolved 2026-04-05
- [x] **zhengyi-he-onsite: 7 remaining agent branches** — closed out 2026-04-05
- [x] **Design and initialize AIBrain repo** — completed 2026-03-28
- [x] **mockFinch: Claude Files API integration** — all 3 endpoints validated with live Anthropic API, completed 2026-03-30
- [x] **mockFinch: Transcript intake service** — S3 + Anthropic Files upload + Claude extraction, completed 2026-03-30
- [x] **WorkTracking: All 4 phases + 8 backlog items** — full app complete (server, daemon, dashboards, LLM pipeline), completed 2026-03-28
- [x] **mockFinch: 80 E2E tests, RBAC, Temporal, analytics, admin, portal** — completed 2026-03-27

---

## Reference: Skills vs Connectors vs MCP

**MCP** — Open protocol (the plumbing) that lets Claude talk to external services. Each MCP server exposes tools.
**Connectors** — Pre-built MCP integrations with auth handled for you (e.g., Gmail, Notion, Google Calendar).
**Skills** — Prompt-based instructions that teach Claude how to do specific tasks well (e.g., make a polished .pptx).
**Plugins** — Bundles of connectors + skills in one installable package.
